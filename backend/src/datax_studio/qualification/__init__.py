"""Private qualification-only components.

Nothing in this package is imported by the standard API, Worker, Compose
configuration, or public plugin-capability path.  ADR-0011 reserves it for a
future protected Phase-A harness; it must never become a normal-user execution
or release-certification bypass.
"""
