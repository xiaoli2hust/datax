# DataX Enterprise Studio 产品信息架构与 UI 设计

| 项 | 内容 |
|---|---|
| 文档版本 | 1.0 |
| 状态 | V1 实施基线 |
| 基线日期 | 2026-07-30 |
| 设计对象 | Vue 3 + TypeScript + Element Plus 企业后台 |
| 关联文档 | `00_文档总览与决策基线.md`、`01_产品战略与PRD.md`、`05_API接口与前后端契约.md`、`09_测试与验收标准.md` |

## 1. 设计目标与边界

V1 UI 服务于一条明确的离线批同步闭环：

`登录 → 进入项目 → 创建并测试数据源 → 创建任务草稿 → 字段映射 → 校验 → 预览脱敏 JSON → 发布版本 → 手动运行 → 查看状态和日志 → 取消或人工重跑 → 查看审计`

### 1.1 设计原则

1. **事实优先**：界面只展示后端已确认的事实。提交执行后显示“已提交/排队中”，不得提前显示“成功”。
2. **项目先行**：除组织级系统管理外，页面始终处于明确项目上下文；项目切换后数据、权限和统计一起切换。
3. **高信息密度但可扫描**：列表支持筛选、稳定排序、分页和列管理；详情用摘要、时间线和页签分层。
4. **强反馈**：长操作有阶段、耗时、取消或重试入口；错误包含原因、下一步动作、稳定错误码和 `request_id`。
5. **风险可见**：`insert-only`、全表读取、重复运行、取消不等于回滚等风险必须出现在动作发生前。
6. **安全默认**：密码、完整连接串、运行时 JSON 和敏感日志不进入浏览器；服务端授权是最终边界。
7. **不伪装能力**：V1 不出现 AI、调度、DAG、告警、插件市场、CDC 或自定义 SQL 的可用入口和假结果。
8. **离线批语义**：日志增量刷新可称为“运行日志更新”，不能称为“实时数据同步”。

### 1.2 视觉方向

- 桌面优先的企业控制台，浅色主题为 V1 默认。
- 信息色用于普通提示，成功/警告/危险色只表达状态，不承担唯一识别作用。
- 页面标题、项目、资源名称和当前状态形成稳定的视觉层级。
- 表格默认紧凑密度；表单使用舒适密度；高风险确认采用独立对话框。
- Logo 与品牌色在品牌验收前使用中性标识，不复制第三方商标。

## 2. 全局信息架构

### 2.1 应用框架

| 区域 | 内容 | 规则 |
|---|---|---|
| 顶栏左侧 | 产品标识、当前项目选择器 | 项目内页面始终显示当前项目；切换前处理未保存内容 |
| 顶栏右侧 | API/Worker 健康状态入口、帮助、用户菜单 | 不提供 V1 无告警能力的“通知中心” |
| 左侧主导航 | 首页、数据源、同步任务、运行中心、审计日志 | 根据项目权限展示；隐藏入口不替代服务端鉴权 |
| 左侧管理导航 | 项目管理、用户与授权 | 仅 Admin 显示 |
| 内容区 | 面包屑、页面标题、说明、主操作、页面主体 | 主操作每页最多一个高强调按钮 |
| 全局反馈层 | Toast、消息条、确认框、错误抽屉 | Toast 不承载必须保留的错误详情 |

### 2.2 导航树

```text
登录
└── 项目工作区
    ├── 首页
    ├── 数据源
    │   ├── 数据源列表
    │   ├── 新建数据源
    │   └── 数据源详情 / 编辑
    ├── 同步任务
    │   ├── 任务列表
    │   ├── 新建 / 编辑任务向导
    │   ├── 任务详情
    │   ├── 版本详情 / 对比
    │   └── 脱敏 JSON 预览
    ├── 运行中心
    │   ├── 执行列表
    │   └── 执行详情 / 日志
    ├── 审计日志
    │   ├── 项目审计
    │   └── 全组织审计（Admin）
    └── 系统管理（Admin）
        ├── 项目管理
        └── 用户与授权

用户菜单
├── 个人资料与时区
├── 修改密码
└── 退出登录
```

以下菜单在 V1 中不存在：插件中心、工作流、调度中心、告警/通知、AI 助手、CDC、数据治理和自定义脚本。

### 2.3 路由与页面清单

| 页面 | 建议路由 | 主要角色 | 主要动作 |
|---|---|---|---|
| 登录 | `/login` | 所有未登录用户 | 登录 |
| 首页 | `/p/:projectId/home` | 所有项目成员 | 查看、下钻 |
| 数据源列表 | `/p/:projectId/datasources` | 所有项目成员 | 筛选；Admin/Developer 新建 |
| 新建数据源 | `/p/:projectId/datasources/new` | Admin、Developer | 填写、测试、保存 |
| 数据源详情/编辑 | `/p/:projectId/datasources/:id` | 所有项目成员 | 查看；Admin/Developer 编辑、测试、停用/归档 |
| 任务列表 | `/p/:projectId/jobs` | 所有项目成员 | 筛选；Admin/Developer 新建 |
| 任务向导 | `/p/:projectId/jobs/new`、`/jobs/:id/edit` | Admin、Developer | 保存草稿、校验、发布 |
| 任务详情 | `/p/:projectId/jobs/:id` | 所有项目成员 | 查看摘要、版本、执行；按权限编辑/归档 |
| 版本详情/对比 | `/p/:projectId/jobs/:id/versions/:versionId` | 所有项目成员 | 查看、对比、预览脱敏 JSON |
| 执行列表 | `/p/:projectId/executions` | 所有项目成员 | 筛选、查看；Admin/Operator 运行相关操作 |
| 执行详情 | `/p/:projectId/executions/:id` | 所有项目成员 | 查看日志/指标；Admin/Operator 取消或重跑 |
| 项目审计 | `/p/:projectId/audit` | 所有项目成员 | 查看本项目审计 |
| 项目管理 | `/admin/projects` | Admin | 新建、编辑、归档项目 |
| 用户与授权 | `/admin/users` | Admin | 新建/停用用户、授权、重置密码 |
| 个人资料 | `/profile` | 所有登录用户 | 时区、修改密码 |
| 无权限 | `/403` | 所有用户 | 返回可访问页面 |
| 未找到 | `/404` | 所有用户 | 返回首页 |
| 服务异常 | `/error` | 所有用户 | 重试、复制 request_id |

## 3. 权限在界面中的表现

| 操作 | Admin | Developer | Operator | Viewer |
|---|:---:|:---:|:---:|:---:|
| 项目/用户管理入口 | 显示 | 隐藏 | 隐藏 | 隐藏 |
| 数据源新建、编辑、测试、归档 | 显示 | 显示 | 隐藏 | 隐藏 |
| 任务新建、编辑、校验、发布、归档 | 显示 | 显示 | 隐藏 | 隐藏 |
| 运行、取消、人工重跑 | 显示 | 隐藏 | 显示 | 隐藏 |
| 数据源/任务/版本/执行/日志查看 | 显示 | 显示 | 显示 | 显示 |
| 项目审计查看 | 显示 | 显示 | 显示 | 显示 |
| 全组织审计查看 | 显示 | 隐藏 | 隐藏 | 隐藏 |

交互规则：

- 用户没有某动作权限时，默认隐藏动作；若为了理解流程而保留，则禁用并说明“需要 Operator 角色”，不得只显示无解释的灰色按钮。
- 通过 URL 直接访问无权页面时进入 403；资源标识不属于当前项目时不能泄露其名称、存在性或属性。
- 角色在用户停留页面期间被撤销时，下一次请求返回 403，界面清除敏感缓存并提示刷新权限。
- 多角色按当前项目取并集。例如同时拥有 Developer 与 Operator 的用户可开发、发布并运行。

## 4. 全局交互与页面状态

### 4.1 标准页面结构

每个列表页按以下顺序组织：

1. 面包屑、页面标题和一句范围说明。
2. 右上角主操作。
3. 筛选条：关键词、状态、类型、时间范围和“重置”。
4. 结果摘要：当前项目、筛选条件、总数和最后刷新时间。
5. 表格：稳定默认排序、分页、列设置和行操作。
6. 空态、错误态或内容。

每个详情页按以下顺序组织：

1. 返回入口、资源名称、状态徽标和主动作。
2. 关键摘要：项目、版本、负责人、时间、关联资源。
3. 风险或异常消息条。
4. 页签内容。
5. 审计和关联对象深链。

### 4.2 通用状态规范

| 状态 | UI 行为 | 禁止行为 |
|---|---|---|
| 首次加载 | 使用与最终布局一致的 Skeleton；危险动作禁用 | 用全屏转圈遮住全局导航；展示旧项目数据 |
| 后台刷新 | 保留现有内容，显示“更新中”和最后更新时间 | 清空列表造成跳动 |
| 无数据 | 说明当前范围并给有权限用户一个下一步动作 | 用演示数据填充 |
| 筛选无结果 | 显示当前筛选摘要和“清除筛选” | 当成系统无数据 |
| 可恢复错误 | 保留安全的旧内容，显示原因、重试和 request_id | 吞错后显示成功 |
| 首屏失败 | 展示错误页、重试、帮助信息和 request_id | 永久 Loading |
| 无权限 | 403 页面或局部权限态，给返回入口 | 暴露资源名称、密钥或跨项目信息 |
| 会话过期 | 保存非敏感本地草稿提示，跳转登录；登录后回原路由 | 静默丢失输入 |
| 网络断开 | 顶部持续消息条“连接已中断”；读内容标记可能过期 | 把未确认写操作显示为成功 |
| 乐观锁冲突 | 409 冲突页展示“重新加载”和安全的只读差异摘要 | 静默覆盖他人修改 |
| 服务降级 | 分别标识 API、数据库、Redis、Worker、Runtime 状态 | 只显示笼统“系统异常” |

### 4.3 表格与筛选

- 列表必须服务端分页，默认每页 20 条，可选 20/50/100。
- 默认排序必须稳定；同一时间戳使用不可变 ID 作为次级排序键。
- URL 保存筛选、页码和排序，刷新与返回后可恢复。
- 关键词搜索明确搜索字段，不提供未定义的“全局万能搜索”。
- 批量高风险操作不进入 V1；用户逐项确认，降低跨项目误操作。
- 空值显示“—”，未采集指标显示“未采集”，不得把缺失值显示为 `0`。

## 5. 页面详细设计

### 5.1 登录

**字段**

| 字段 | 控件 | 校验 |
|---|---|---|
| 登录邮箱 | 邮箱输入框 | 必填；合法邮箱；最长 254 字符；前后空格自动去除并按服务端规则归一 |
| 密码 | 密码框 | 必填；不提供默认值；支持按住显示 |

**交互**

- Enter 提交；提交期间按钮显示“登录中…”且防重复。
- 认证失败统一显示“邮箱或密码不正确”，防止账号枚举。
- 服务不可用时显示“暂时无法连接服务”，附 request_id 和重试。
- 登录成功后进入上次可访问项目；无项目时进入明确的无项目状态。
- Admin 无项目时显示“创建第一个项目”；普通用户显示“尚未被分配项目，请联系管理员”。

### 5.2 首页

**页面内容**

- 范围条：当前项目、时间范围（默认最近 24 小时）、最后刷新时间。
- 指标卡：已发布任务、24 小时执行数、成功率、运行中、排队中、失败待处理。
- 7 日执行趋势：成功、失败、超时、丢失、取消；提供同数据表格替代。
- 最近执行：执行编号、任务、版本、状态、触发人、开始时间、耗时。
- 待处理失败：仅为筛选视图，不是告警能力；点击进入运行中心。
- Runtime 状态摘要：Worker 是否在线、固定 DataX 版本是否匹配；不显示主机敏感信息。

**指标规则**

- 指标严格采用 `01_产品战略与PRD.md` 的口径。
- 成功率无分母时显示“—”，辅助文本为“当前范围内无已结束执行”。
- 卡片下钻必须携带相同项目、时间和状态筛选。
- Worker 离线时显示持续警告，运行入口禁用并解释原因。

### 5.3 项目管理（Admin）

**列表字段**

项目名称、项目标识、状态、成员数、任务数、最近执行时间、创建时间、操作。

**编辑字段**

| 字段 | 校验 |
|---|---|
| 项目名称 | 必填；2–128 字符；同组织内唯一 |
| 项目标识（slug） | 必填；3–64 字符；`[a-z][a-z0-9-]*`；创建后不可修改 |
| 描述 | 0–1000 字符 |
| 状态 | `ACTIVE` 或 `ARCHIVED`；归档通过独立危险操作完成 |

**状态和约束**

- 无项目空态显示范围说明和“新建项目”。
- 归档前检查 `QUEUED/STARTING/RUNNING/CANCEL_REQUESTED` 执行；存在时阻断并给下钻链接。
- 归档确认必须输入项目标识；归档后只读，历史资源与审计保留。
- 项目不提供物理删除按钮。

### 5.4 用户与授权（Admin）

**列表字段**

登录邮箱、显示名、状态、项目数、角色摘要、上次登录时间、创建时间、操作。

**编辑字段**

| 字段 | 校验 |
|---|---|
| 登录邮箱 | 必填；合法邮箱；最长 254 字符；创建后不可修改 |
| 显示名 | 必填；1–128 字符 |
| 初始密码/新密码 | 至少 12 字符并满足服务端密码策略；不回显 |
| 状态 | `ACTIVE`、`LOCKED`、`DISABLED`；`LOCKED` 由安全策略产生，不作为普通手工选择 |
| 项目与角色 | 每个项目可多选 Developer/Operator/Viewer；Admin 为组织级授权 |

**交互**

- 停用用户前显示其运行中执行；停用账号不自动取消已在 Worker 中运行的进程。
- 重置密码后只显示一次临时凭据或安全重置结果；页面刷新后不可再次读取。
- 角色变更对话框按项目分组，保存前展示新增和移除差异。
- 用户不能停用自己的最后一个 Admin 账号；服务端和 UI 双重阻断。

### 5.5 数据源列表

**列表字段**

名称、类型、主机（脱敏）、数据库、状态、最近连接测试、引用任务数、更新人、更新时间、操作。

**筛选**

关键词（名称/主机）、类型（MySQL/PostgreSQL）、状态、最近测试结果。

**动作**

- Admin/Developer：新建、编辑、测试连接、停用、归档。
- Operator/Viewer：查看非敏感详情。
- 行内测试为异步动作，显示“测试中…”；用户离开页面后结果仍以服务端记录为准。

**空态**

- 有编辑权限：“当前项目还没有数据源。创建第一个 MySQL 或 PostgreSQL 数据源。”
- 无编辑权限：“当前项目还没有可用数据源，请联系项目 Developer 或 Admin。”

### 5.6 新建/编辑数据源

**字段**

| 字段 | 控件与校验 |
|---|---|
| 名称 | 必填；2–128 字符；项目内唯一 |
| 类型 | MySQL 8 / PostgreSQL 15；创建后不可修改 |
| 主机 | 必填；仅主机名或 IP，不含协议、路径和 JDBC 参数 |
| 端口 | 必填；1–65535；MySQL 默认 3306，PostgreSQL 默认 5432 |
| 数据库 | 必填；1–128 字符；不接受 SQL 或连接串 |
| 默认 Schema | PostgreSQL 必填，默认 `public`；MySQL 隐藏该项 |
| 用户名 | 必填；1–128 字符 |
| 密码 | 新建必填；编辑显示“已保存”，用户未输入新值时前端不提交该字段；`null` 或空字符串不能发送为新密码 |
| SSL 模式 | `DISABLE`、`REQUIRE`、`VERIFY_CA`、`VERIFY_FULL`；默认及允许值由部署安全策略约束 |
| 描述 | 0–1000 字符 |

平台从受控字段生成 JDBC URL。`VERIFY_CA/VERIFY_FULL` 使用部署方预置的信任库；V1 不上传证书文件。仅可呈现插件 Manifest 白名单内的非敏感枚举项，不提供自由连接串、任意 JDBC 参数、SQL 初始化脚本、证书路径或插件选择输入框。

**交互顺序**

1. 用户填写字段。
2. 点击“测试连接”；界面显示阶段、耗时和可取消的 UI 等待状态，但不声称已取消后端测试。
3. 成功时显示数据库类型/版本摘要和测试时间；失败时显示安全错误摘要、建议动作、错误码和 request_id。
4. 允许保存未测试草稿，但该数据源标记“未验证”，不能用于任务发布。
5. 编辑连接字段后，先前测试结果标记“已过期”，必须重新测试。

**危险状态**

- 停用：确认后阻止新发布和新执行；历史可见。
- 凭据轮换：要求输入新密码并重新测试；永不显示旧密码。
- 归档：有已发布任务引用时阻断并列出任务链接；无引用时二次确认。

### 5.7 同步任务列表

**列表字段**

任务名称、状态、最新发布版本、Reader → Writer、源表 → 目标表、最近执行状态、最近执行时间、更新人、更新时间、操作。

**筛选**

关键词（任务名/表名）、任务状态、Reader 类型、Writer 类型、最近执行状态。

**状态动作**

- `DRAFT`：Admin/Developer 可继续编辑、校验；若已有历史发布版本，Admin/Operator 仍可显式运行该不可变版本。
- `VALID`：Admin/Developer 可发布；任意配置编辑后回到 `DRAFT`；若已有历史发布版本仍可显式运行。
- `PUBLISHED`：Admin/Operator 可运行最新发布版本；Admin/Developer 可“基于当前版本编辑”。
- `ARCHIVED`：只读，不显示运行按钮。

列表中的运行按钮只在任务未归档、至少存在一个已发布版本、用户具备 Admin 或 Operator 任一权限且 Worker 可用时显示。任务处于 `DRAFT/VALID` 且已有旧版本时，按钮文案为“运行已发布的 vN”，避免误以为运行当前草稿。

### 5.8 新建/编辑任务向导

向导共五步，不包含调度、DAG、告警、AI、SQL 或插件选择。

#### 步骤 1：基本信息

| 字段 | 校验 |
|---|---|
| 任务名称 | 必填；2–128 字符；项目内唯一 |
| 描述 | 0–1000 字符 |

- 创建即生成 `DRAFT`。
- 编辑已发布任务时，界面明确“正在创建新草稿，历史版本不会改变”。

#### 步骤 2：Reader 与源字段

| 字段 | 规则 |
|---|---|
| Reader 数据源 | 仅当前项目、已启用、测试有效的 MySQL/PostgreSQL |
| Schema | PostgreSQL 必选；MySQL 使用数据库范围 |
| 源表 | 单选；支持服务端搜索和分页 |
| 字段模式 | “全部字段”或“选择字段” |
| 源字段 | 至少 1 个、最多 2048 个；选列模式显式勾选；显示名称、类型、长度/精度、可空、主键 |

- 表和字段来自真实元数据接口，不允许自由输入表名、SQL 或通配符。
- 更换数据源或源表会清空下游映射；确认框列出将丢失的步骤数据。
- 元数据加载失败保留已保存标识，但禁止继续校验并提供重试。

#### 步骤 3：Writer 与字段映射

| 字段 | 规则 |
|---|---|
| Writer 数据源 | 仅当前项目、已启用、测试有效的 MySQL/PostgreSQL |
| Schema | PostgreSQL 必选；MySQL 使用数据库范围 |
| 目标表 | 必须从真实元数据中选择，不能创建新表 |
| 字段映射 | 每个源字段恰好映射一个不重复目标字段 |

映射表显示：

- 顺序；
- 源字段名、源类型、可空；
- 目标字段选择、目标类型、可空/默认值；
- 兼容结果：兼容、警告、阻断；
- 具体原因和修复建议。

交互规则：

- 首次加载按同名且兼容推荐，推荐不等于自动通过。
- 支持上移/下移按钮和键盘操作，不以拖拽作为唯一排序方式。
- 同一目标字段被重复选择时立即阻断。
- 目标必填字段未映射且无默认值时阻断。
- 源与目标为同一数据源、同一 Schema、同一表时阻断。
- 不支持的类型、缩窄、长度/精度不足或时区语义不明时阻断；不做隐式截断或猜测。

#### 步骤 4：运行参数

| 字段 | 默认值 | 校验 |
|---|---:|---|
| Channel | 1 | 整数，1–16 |
| 脏数据最大条数 | 0 | 整数，0–1,000,000；`0` 表示不允许脏数据 |
| 脏数据最大比例 | 0% | 0–100%，最多 4 位小数；`0%` 表示不允许脏数据 |
| 执行超时 | 60 分钟 | 整数，1–10,080 分钟（60–604,800 秒） |

- UI 将脏数据比例的百分数换算为契约中的 `0..1` 小数，回显时使用同一精度，不能因舍入扩大阈值。
- 写入模式固定显示为只读 `insert-only`。
- 读取范围固定显示为“每次运行读取源表当前全部行”；V1 无 where 条件或水位线。
- 不显示 batchSize、速度字节阈值、错误重试、自定义 JSON 等未开放参数。

#### 步骤 5：校验、预览与发布

页面从上到下展示：

1. 配置摘要：Reader/Writer、表、字段数、`channel`、阈值、超时。
2. 风险消息：“目标表必须预先存在；本任务仅追加写入；重复运行可能产生重复数据或唯一键冲突。”
3. 校验清单：权限、数据源有效性、表/字段存在性、类型兼容、目标约束和参数。
4. 问题列表：错误、警告、所属步骤、字段和“前往修复”。
5. 脱敏 DataX JSON：只读、格式化、可复制脱敏内容；禁止编辑或下载含密钥版本。
6. “发布版本”主按钮：仅校验通过且无配置变化时启用。

发布确认文案必须包含新版本号、配置哈希短码、Reader/Writer 摘要和“发布后版本不可修改”。发布成功后进入任务详情，而不是自动运行。

#### 向导通用行为

- 每一步可“保存草稿并退出”；自动保存只能保存已通过本地校验的非敏感表单值。
- 页签、刷新和返回前提示未保存更改；不得把密码、完整连接串或完整运行 JSON 存入浏览器持久存储。
- 后退保留已保存数据；更改上游选择时明确清理受影响的下游数据。
- 保存发生 409 时显示当前版本与服务器版本摘要，用户选择重新加载或复制自己输入，不能静默合并。
- 校验期间按钮显示进度并防重复；超时后允许重试，不生成“通过”状态。

### 5.9 任务详情与版本

**页头**

任务名称、状态、Reader → Writer、最新发布版本、更新人/时间，以及按权限显示的编辑、发布、运行、归档动作。

**页签**

| 页签 | 内容 |
|---|---|
| 概览 | 数据源、表、字段数、参数、最新执行、风险摘要 |
| 当前草稿 | 草稿状态、上次校验、问题数；仅 Admin/Developer 可编辑 |
| 已发布版本 | 版本号、哈希、发布人、发布时间、Runtime/插件版本 |
| 执行历史 | 该任务的执行列表与状态 |
| 审计 | 该任务相关审计事件 |

**版本对比**

- 对比基本信息、Reader/Writer、表、字段增删/映射、参数和配置哈希。
- 敏感字段统一显示 `••••••`；相同密文变化也只显示“凭据引用已变更”。
- 历史版本完全只读；不提供直接修改或覆盖入口。

### 5.10 运行前确认

运行由 Admin 或 Operator 发起。对话框展示：

- 任务名和 JobVersion；
- 源表、目标表和字段数；
- `insert-only` 固定写入方式；
- 全表读取和重复运行风险；
- 目标表预存在责任；
- 执行超时；

主按钮文案为“确认并提交执行”，不能使用“开始同步成功”。提交后：

- 服务端返回 Execution ID，跳转执行详情；
- 相同幂等请求只显示同一执行；
- 未收到明确响应时显示“提交结果待确认”，通过幂等查询确认，不能让用户盲目重复创建。

### 5.11 运行中心

**列表字段**

执行编号、任务、版本、状态、触发人、排队时间、开始时间、结束时间、耗时、读取/写入记录数、脏数据数、操作。

**筛选**

关键词（执行编号/任务名）、状态、任务、版本、触发人、时间范围、是否为重跑。

**自动刷新**

- V1 单 Worker 默认同时运行 1 个 Execution；超出部署并发上限的执行保持“排队中”，列表不得暗示卡死。
- 默认每 5 秒增量刷新活动执行；用户可暂停。
- 页面不可见时降低刷新频率，恢复可见时立即对账。
- 新状态未确认前保留旧状态和“更新中”标识，不猜测终态。
- 断线时显示持续消息条；恢复后从服务端重新获取事实状态。

### 5.12 执行详情与日志

**页头与摘要**

- 执行编号、状态徽标、任务、JobVersion、触发人、触发时间。
- 配置哈希、Runtime 版本、插件版本。
- 排队、启动、运行、结束时间和耗时。
- 人工重跑来源或后继执行链接。
- Admin/Operator 按状态看到“取消”或“人工重跑”。

**状态时间线**

逐项展示状态、时间、来源（API/Worker/对账）、安全摘要。`LOST` 必须解释为“平台无法确认 DataX 最终结果，请先核对目标表再决定是否重跑”，不能归入普通失败。

**指标**

读取记录数、写入记录数、字节数、平均速率、脏数据条数/比例、DataX 退出码。没有采集到的值显示“未采集”。

**错误区**

稳定错误码、人类可读摘要、可能原因、建议动作、request_id、脱敏技术详情。目标端可能已经部分写入时显示持久警告。

**日志查看器**

- 默认只显示脱敏文本；HTML 以文本转义，ANSI 控制字符不执行。
- 使用单调游标增量读取，显示“已连接/重连中/已暂停/已结束”。
- 支持自动滚动开关、暂停、继续、关键词搜索、复制选中内容和下载脱敏日志。
- 大日志使用虚拟列表或分块加载，不能一次把全量日志放入 DOM。
- 日志被保留策略清理时显示“日志已按保留策略清理”，执行元数据仍可查看。
- 默认保留口径为：脱敏运行日志 30 天，Execution/ExecutionEvent 365 天；页面显示按服务端返回计算的实际到期时间。
- 日志流断开不改变执行状态；重连从最后确认游标继续。

**取消确认**

文案：

> 取消会请求 Worker 停止 DataX 进程，但不能撤销已提交到目标库的数据。是否继续？

确认后按钮进入“取消请求已提交”，直到 Worker 写入 `CANCELED` 或其他终态。不得在 API 接受请求时直接显示“已取消”。

**人工重跑确认**

仅对 `FAILED / TIMED_OUT / CANCELED / LOST` 显示。必须再次展示 `insert-only` 和部分写入风险，确认后创建新 Execution 并跳转，不覆盖原执行。

### 5.13 审计日志

**项目审计字段**

时间、操作者、角色、动作、对象类型、对象名称/ID、结果、来源 IP 摘要、request_id。

**筛选**

时间范围、操作者、动作、对象类型、结果。Admin 可在组织级页面增加项目筛选。

**详情**

- 展示脱敏前后差异、错误码和关联资源链接。
- 密码、Token、完整连接串、Runtime JSON 和日志敏感片段不得展示。
- 审计为只读，不提供编辑或删除入口。
- 审计默认保留 730 天；若部署策略依法延长，以服务端返回的保留信息为准。
- 非 Admin 只能查看当前授权项目；跨项目对象以安全方式拒绝。

### 5.14 个人资料

- 显示登录邮箱（不可改）、显示名、项目与角色摘要。
- 默认使用浏览器时区。用户可为当前设备选择展示时区，该非敏感偏好仅保存在本地且 V1 不跨设备同步；所有服务端时间仍为 UTC/RFC 3339。
- 修改密码要求旧密码、新密码和确认密码；成功后提示其他会话已失效。
- 退出登录需清除浏览器中的会话和非必要项目缓存。

### 5.15 403、404 与服务异常

- 403：“你没有访问此页面的权限。”提供返回当前项目首页，不展示目标资源名称。
- 404：“页面不存在或已被移除。”无权限资源也可按安全策略返回 404。
- 401：跳转登录，并保留安全的回跳路径。
- 500/503：展示安全摘要、错误码、request_id、发生时间、重试和复制诊断信息。
- Worker/Runtime 不可用属于局部服务异常：只禁用新运行，任务查看和历史记录仍可用。

## 6. 状态、徽标与术语

### 6.1 任务状态

| 后端值 | 中文标签 | 颜色/图标语义 | 可用动作 |
|---|---|---|---|
| `DRAFT` | 草稿 | 中性 + 编辑图标 | 编辑、校验；若有历史发布版本，可按权限运行该版本 |
| `VALID` | 已校验 | 信息色 + 对勾轮廓 | 发布、继续编辑；若有历史发布版本，可按权限运行该版本 |
| `PUBLISHED` | 已发布 | 成功色 + 版本图标 | 运行（Admin/Operator）、基于版本编辑、归档 |
| `ARCHIVED` | 已归档 | 中性 + 归档图标 | 只读查看 |

### 6.2 执行状态

| 后端值 | 中文标签 | UI 说明 |
|---|---|---|
| `QUEUED` | 排队中 | 已创建执行，等待 Worker 资源 |
| `STARTING` | 启动中 | Worker 正在准备工作目录和启动 Runtime |
| `RUNNING` | 运行中 | DataX 进程运行，日志持续更新 |
| `CANCEL_REQUESTED` | 取消中 | 已接受请求，等待 Worker 终止并对账 |
| `SUCCEEDED` | 成功 | DataX 退出码为 0；指标解析状态单独展示 |
| `FAILED` | 失败 | 已确认失败；显示错误码和处置建议 |
| `TIMED_OUT` | 已超时 | 超过配置时间并由 Worker 终止/对账 |
| `CANCELED` | 已取消 | Worker 已确认取消终态；不代表目标数据回滚 |
| `LOST` | 状态丢失 | 无法确认最终结果；必须先核对目标表 |

状态必须同时用文字、图标和颜色表达。表格、图表、Toast 和详情页使用同一中文标签，不得混用“终止/停止/取消成功”等不同语义。

## 7. 校验与错误呈现

### 7.1 校验层级

1. **输入时校验**：格式、长度、范围和必填项。
2. **离开字段校验**：名称唯一性、端口和组合字段。
3. **服务端保存校验**：权限、项目边界、乐观锁和资源状态。
4. **任务校验**：真实连接、元数据、映射、类型、约束、参数和 Runtime 能力。
5. **运行前校验**：发布版本、数据源状态、Schema 漂移、Worker/Runtime 可用性和幂等键。

客户端校验用于及时反馈，不能替代服务端校验。

### 7.2 错误消息结构

错误消息统一包含：

- **发生了什么**：例如“目标字段 `amount` 精度不足”；
- **为什么**：例如“源字段为 DECIMAL(18,2)，目标字段为 NUMERIC(10,2)”；
- **如何处理**：例如“请预先扩大目标字段或选择其他目标字段”；
- **定位信息**：步骤、字段、稳定错误码和 request_id。

禁止使用：

- “操作失败，请重试”作为唯一信息；
- 数据库原始异常、堆栈、完整 JDBC URL 或明文凭据；
- 把未知、超时或断线显示成成功；
- 自动改写用户映射后不提示。

## 8. 危险操作与防误触

| 操作 | 风险 | UI 保护 |
|---|---|---|
| 运行 | 全表 append，可能重复或冲突 | 展示版本、表、insert-only 风险；明确确认 |
| 人工重跑 | 原执行可能已部分写入 | 展示原执行和部分写入警告；创建新执行 |
| 取消 | 不能回滚已提交数据 | 明确文案；请求态与最终取消态分离 |
| 更换 Reader/源表 | 清空字段选择和映射 | 列出受影响内容，确认后清空 |
| 更换 Writer/目标表 | 清空目标映射 | 列出受影响内容，确认后清空 |
| 发布 | 生成不可变版本 | 展示版本号、哈希短码和配置摘要 |
| 凭据轮换 | 可能使任务不可执行 | 新密码不可回读；必须重新测试连接 |
| 停用数据源 | 阻止新发布和执行 | 展示引用任务；确认后停用 |
| 归档任务 | 禁止新执行 | 存在活动执行时阻断；历史保留 |
| 归档项目 | 整个项目只读 | 输入项目标识确认；活动执行时阻断 |
| 停用用户 | 用户无法建立新会话 | 展示角色和活动执行；不能停用最后一个 Admin |

高风险操作不得放在行首主按钮；使用明确动词，不使用模糊的“确定”。二次确认不能用颜色作为唯一风险提示。

## 9. 响应式与可访问性

### 9.1 响应式策略

| 视口 | 行为 |
|---|---|
| ≥ 1440 px | 完整侧栏、完整表格和双栏详情 |
| 1280–1439 px | 完整核心流程；次要表格列可收起到列设置 |
| 1024–1279 px | 侧栏折叠；表单单列；表格允许受控横向滚动；任务映射保持可编辑 |
| 768–1023 px | 支持查看、运行监控和表单；复杂字段映射使用全屏步骤页 |
| < 768 px | 支持登录、首页摘要、执行列表/详情、取消和重跑；任务/数据源编辑显示“请使用宽度至少 768 px 的设备”，不呈现损坏编辑器 |

验收至少覆盖 1440×900、1280×720、1024×768 和 390×844。任何视口都不能因溢出遮挡退出登录、返回、错误信息或运行中的取消动作。

### 9.2 可访问性要求

- 核心流程满足 WCAG 2.1 AA 对比度；正文至少 4.5:1，大文本至少 3:1。
- 所有控件有可读标签、帮助文本和错误关联；占位符不能替代标签。
- 键盘可完成登录、数据源表单、任务五步向导、发布、运行确认、日志控制和取消。
- 拖拽排序必须提供上移/下移按钮；焦点顺序与视觉顺序一致。
- 对话框打开后聚焦标题或首个控件，焦点锁定，关闭后返回触发元素。
- 状态变化通过 `aria-live` 适度播报；日志流不逐行抢占读屏器。
- 图表提供同口径表格或摘要；状态不只依赖颜色。
- 可见焦点不能被主题覆盖；触控目标建议不小于 44×44 px。
- 用户选择“减少动态效果”时关闭非必要动画。

## 10. 文案规范

### 10.1 术语统一

| 使用 | 不使用 |
|---|---|
| 离线批同步 | 实时同步 |
| 同步任务 | 作业、流程、工作流混用 |
| 已发布版本 | 已上线、生产版本（V1 无环境发布语义） |
| 执行 | 实例、任务运行混用 |
| 排队中 / 启动中 / 运行中 | 正在同步（无法区分阶段） |
| 人工重跑 | 自动重试 |
| 脱敏 JSON 预览 | 完整 JSON |
| 状态丢失 | 默认归类为失败或成功 |

### 10.2 操作反馈示例

- 保存草稿成功：“草稿已保存，尚未校验。”
- 校验成功：“校验通过，可发布新版本。”
- 发布成功：“版本 v3 已发布。发布不会自动运行任务。”
- 运行提交成功：“执行 `EXE-20260730-0012` 已提交，当前排队中。”
- 取消请求成功：“取消请求已提交，等待 Worker 确认。”
- 网络不确定：“暂未确认提交结果，正在使用请求标识查询，请勿重复提交。”
- Worker 离线：“执行 Worker 当前不可用，暂不能提交新执行；历史记录仍可查看。”
- 日志清理：“运行日志已按保留策略清理，执行摘要和审计记录仍保留。”

Toast 仅显示短结果；失败详情、风险和需要用户处理的内容必须在页面消息条或错误抽屉中持续可见。

## 11. UI 数据安全

- 浏览器不接收已保存密码、完整连接串、主密钥、完整运行 JSON 或未脱敏日志。
- 密码输入禁止写入 URL、本地存储、会话存储、分析事件和错误上报。
- JSON 预览和版本对比基于后端脱敏表示，不在前端用字符串替换“补做脱敏”。
- 表名、字段名、日志、错误和审计差异全部按不可信文本转义。
- 下载日志必须由服务端生成脱敏文件，并带项目授权和审计。
- 切换项目、退出或权限撤销后，清理对应查询缓存和敏感页面状态。
- 前端不提供任意 SQL、JDBC URL、自定义参数、文件路径或插件上传输入。

## 12. UI 验收标准

### 12.1 功能与状态

| 验收 ID | 验收场景 | 通过条件 |
|---|---|---|
| ACC-UI-001 | 四类角色分别登录并访问所有导航、深链和动作 | 菜单、按钮、页面和 API 行为符合权限矩阵；无跨项目信息泄露 |
| ACC-UI-002 | Admin 创建项目、用户并授权；Developer 创建/测试数据源、创建任务、校验和发布；Operator 运行；Viewer 查看 | 全流程可完成且角色切换清晰；任何步骤不依赖手改 JSON、SQL、AI 或服务器终端 |
| ACC-UI-003 | 数据源测试成功、认证失败、网络超时、服务异常 | 每种状态有不同文案、错误码、request_id 和重试；密码/连接串不泄露 |
| ACC-UI-004 | 任务向导保存、后退、刷新、离开、上游选择改变和 409 冲突 | 草稿不静默丢失或覆盖；受影响映射在确认后清理；冲突可恢复 |
| ACC-UI-005 | 映射缺失、重复、不兼容、精度不足、目标非空无默认值 | 问题定位到字段并阻断发布；修复后可重新校验 |
| ACC-UI-006 | 双击运行、网络响应丢失和相同幂等键重试 | 只出现一个 Execution；未确认时不显示成功并防止盲目重复 |
| ACC-UI-007 | 执行遍历所有合法状态和取消路径 | 列表、详情、时间线和 Toast 使用统一标签；只有 Worker 终态后显示成功/取消 |
| ACC-UI-008 | 日志断开重连、50,000 行日志、超长行、HTML/ANSI 和敏感样例 | 游标连续、页面可操作、脚本不执行、明文敏感值不可检出、下载仍脱敏 |
| ACC-UI-009 | 每个列表和详情的加载、无数据、筛选无结果、错误、无权限、网络断开和服务降级 | 均有设计态和可执行下一步；无演示数据、永久 Loading 或假成功 |
| ACC-UI-010 | 运行、重跑、取消、发布、轮换凭据、停用和归档 | 均有匹配风险的确认；活动执行和引用约束会阻断不合法操作 |
| ACC-UI-011 | AI、调度、DAG、告警、插件市场、CDC、SQL 和自定义插件范围检查 | 导航、页面、表单、API 错误提示和示例中均无可误认为 V1 可用的入口 |
| ACC-UI-012 | 首页卡片与下钻列表使用相同项目和时间范围 | 数量、成功率分母、状态和时间口径一致；无数据时显示“—”而非 0 |

### 12.2 可用性、性能与可访问性

| 验收 ID | 通过条件 |
|---|---|
| ACC-UI-013 | 在 1440×900、1280×720、1024×768、390×844 下无关键操作被遮挡；小屏限制有明确文案。 |
| ACC-UI-014 | 仅使用键盘可完成登录、数据源创建/测试、任务向导、发布、运行确认、日志操作和取消；焦点无丢失。 |
| ACC-UI-015 | 自动可访问性扫描无 Critical/Serious 问题；人工验证对比度、读屏标签、状态播报、图表替代和非颜色编码。 |
| ACC-UI-016 | 验收数据量下主要页面首次可交互 P95 ≤ 2 秒；后台刷新不清空内容；大日志不阻塞主线程超过可感知阈值。 |
| ACC-UI-017 | 浏览器开发者工具、网络响应、存储、前端日志、错误上报和下载文件中均找不到明文凭据或完整运行 JSON。 |
| ACC-UI-018 | Chrome 与 Edge 当前稳定版、Firefox 当前稳定版完成核心流程；其他浏览器明确标记未认证。 |

### 12.3 设计完成定义

UI 只有在以下条件全部满足时才可交付研发或声明完成：

1. 本文页面均有正常、加载、空、错误、权限和危险操作设计。
2. 字段、枚举、状态、错误码和权限与 API/数据模型契约一致。
3. 核心页面有 1280 px 以上桌面稿，关键窄屏状态和交互说明齐全。
4. 关键流程已用真实接口数据验证；静态点击原型不能替代 DataX E2E。
5. 权限、脱敏、断线、冲突、取消、重跑和辅助技术路径通过测试。
6. 未验证内容在交付记录中明确标记，不能用“企业级”“生产就绪”替代证据。
