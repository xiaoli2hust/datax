# 第一性原理问题台账与开发修复计划

| 项 | 值 |
|---|---|
| 文档版本 | 1.3 |
| 审查快照 | 2026-08-02 |
| 状态 | 权威问题台账；已确认 P0/P1 未归零，V1 完成、试点与发布保持 `BLOCKED` |
| 目标交付 | 签名 Windows `Setup.exe` + 签名 `launcher.exe` + 本机容器化完整产品；不是纯原生单进程 `.exe` |
| 适用范围 | 当前 V1.2 工程候选、后续修复 PR、E3/E4 验收和 ADR-0009 定义的 DataX 能力扩展 |

> 本文管理“当前确认的问题、修复顺序、责任和关闭证据”，不另行改写
> PRD 、Accepted ADR、OpenAPI/JSON Schema 或测试 oracle 的语义。台账与权威来源
> 发生冲突时，对应实现保持 `BLOCKED`，必须先修改关注点的责任文档和机器
> 契约，再修改代码和测试。

## 1. 审查的第一性原理

一个数据复制产品只有同时回答以下问题，才能称为“完成”：

1. **价值是否真实**：目标用户是否真有这个问题，是否愿意承担本产品的
   源静默、空目标和外部独占操作前提。
2. **复制结果是否真实**：DataX 退出码、页面成功提示和产品自报统计都不是
   数据正确性；只有独立 oracle 可以做最终判定。
3. **失败是否安全**：取消、失租、睡眠、断电、恢复中断和部分写入不能被收敛为
   假成功，也不能在不知情时向目标继续追加。
4. **安全边界是否真正强制**：数据库权限、容器网络和 Worker fence 必须由不同
   边界强制，不能只依赖应用层约定。
5. **证据是否能证伪**：验收系统必须校验证据内容及其与需求、测试、候选版本、
   环境和 Execution 的绑定；“任意文件有正确哈希”不能构成 PASS。
6. **交付物是否与承诺一致**：目标是 Windows 可安装产品，不是一个独立 DataX
   命令行文件，也不是只在 macOS/Linux 上可编译的 EXE 骨架。
7. **“支持”是否可验收**：上游源码存在、插件能被编译、运行时已打包、产品
   已开放、真实 E2E 已通过是五个不同状态。

## 2. 当前总体判定

| 维度 | 当前事实 | 状态 | 解锁条件 |
|---|---|---|---|
| 文档体系 | PRD、架构、数据、API、验收、Windows 部署和 ADR 主题已建立 | 可实施候选 | 本台账中的冲突和 P0/P1 必须同步回写责任契约 |
| 产品价值 | 痛点和工作流仍为假设 | `NOT_EVIDENCED` | Discovery Gate 真实证据 |
| 主复制链路 | 四方向实 DataX + 产品链路 + 独立 oracle 未取得 E3 | `NOT_RUN/BLOCKED` | `E2E-DX-001..004` 及失败矩阵的可复现证据 |
| Windows 交付 | 无已签名并通过干净 Windows 11 x64 验收的 Setup/Launcher | `NOT_RUN/BLOCKED` | `E2E-WIN-001/002` E4 证据包 |
| 系统恢复 | 只到双包认证 staging/`LEGACY` 指针候选；真实 restore 未闭环 | `BLOCKED` | 新空卷、`pg_restore`、证据重算、`RESTORE` 代际原子提交和 E4 |
| 审计抗管理员篡改 | 当前只有同库哈希链 | `BLOCKED` | 选定独立信任锚定形态并完成 WORM/签名验收 |
| 发布证据 | Catalog 幽灵 ID 已修复；可信证据、场景 profile 与发布 attestation 仍有 P0 缺陷 | `BLOCKED` | `FP-P0-001` 关闭后重新生成及验证 |
| 对外许可与签名 | 根仓库原创代码许可、发布签名证书尚未由所有者决定/提供 | `OWNER_BLOCKED` | 所有者决策、`LICENSE`、签名与时间戳证据 |

### 2.1 2026-08-01 第一轮开发快照

本文建立后已经开始按 Repair 顺序开发，但下表只记录当前候选实现和本地自动化证据，
不把它们提升为 E3/E4 或 Windows 发布验收：

| 切片 | 已落地候选 | 本地证据 | 仍未关闭 |
|---|---|---|---|
| Repair 0 / catalog | 按表头和单元格解析测试 ID，catalog 从错误的 90 对修正为 81 个需求/85 对，旧 `1.0` manifest 不再兼容 | acceptance 15/15，覆盖错列、多 ID、重复 pair、旧 hash/schema；catalog canonical check | `FP-P0-002` 已在 E1 关闭；可信 runner/候选根属于 `FP-P0-001` |
| Repair 0 / 结构化证据 | E3 oracle 与 E4 Windows JSON 开始绑定 candidate、commit、requirement/test、environment 和 Execution，并校验嵌套文件 hash | acceptance schema/语义回归已拒绝跨 entry 重放 oracle、身份错绑和嵌套 hash 篡改 | 独立复核仍可用自造 candidate/Windows 状态和非结构化通用证据形成非发布结构 PASS；受信 runner/候选根和场景 profile 未完成，`FP-P0-001` 不得关闭 |
| Repair 0 / BLOCKED 候选根 | canonical root 完整盘点候选文件并绑定 Setup/Launcher、最终 publisher-binding manifest 1.1、资源、镜像 lock、SBOM、经权威 Schema/语义验证的 acceptance/environment/catalog；release workflow 增加托管 attestor E1 接线候选，以固定 commit 的 `actions/attest` 签发 root，并把 GitHub CLI 2.97.0 archive/二进制 SHA-256 固定后执行低层反向验证 | candidate-root 12/12；trusted-gh/workflow 8/8 覆盖 archive/binary/version/link、hosted job 权限/action pin、交接 SHA→root→attest 顺序和 blocked artifact；即使低层 policy 全匹配，仍精确要求 `TRUSTED_ATTESTATION_VERIFIER_TCB_NOT_IMPLEMENTED` | workflow 尚未在受保护 Windows runner、真实签名 secrets 和 GitHub attestation 服务上运行；机器场景 profile/result、受保护 Windows E4 harness、wrapper 权威 TCB descriptor、真实 bundle 和完整发布聚合均不存在；`--require-pass` 继续无条件阻断 |
| Repair 1 / restore-start | `Start` 和 `Backup` 在任何 secret、installation-id、卷初始化或备份读取前检查固定 `system-restore` 根并失败关闭 | Rust 52/52、fmt、clippy | 恢复继续与授权清理、journal 篡改/崩溃矩阵和干净 Windows E4 |
| Repair 1 / 核验取消与恢复原子性 | 数据库读取及 SQLite 摘要/差异归并每 1000 条检查 cancel/lease/fence/目标独占；所有非取消终态在 Execution 行锁下拒绝活动取消；撤回/过期稳定进入恢复门禁；`FAILED/TIMED_OUT/CANCELED/LOST + RECOVERY_REQUIRED + RecoveryGate` 在同一事务提交 | Backend 全量 293 passed、7 skipped；摘要/差异中断、FAILED/INCONCLUSIVE 竞态、REVOKED/EXPIRED、四终态 gate、gate 写失败回滚和 lease-LOST 回归通过 | 真实 PostgreSQL 并发/进程终止、真实大表在 VERIFYING 初/中/末批取消和声明撤回/过期的 E3 |
| Repair 2 / 权限契约 | metadata API 增加精确 `SOURCE_USE/TARGET_USE`；通用数据源页显式选择用途且不跨用途复用 cursor；TransferPolicy 列表/详情收敛为 Admin-only | Backend 全量、Ruff、前端 typecheck/build、两用途编译正负例 | 真浏览器四角色/两用途正负例与 E3 操作闭环 |
| Repair 6 / 上游 Reader/Writer inventory 与认证门禁 | 从锁定根 POM、模块 POM 与 `plugin.json` 自动生成 72 项目录（31 Reader、41 Writer）；新增 `plugin-manifest.v2` 和 `/plugins` 能力目录，去除公开 `CERTIFIED`，普通创建/恢复 rerun/Worker claim/start 四检 Windows E4，UI 消费目录并禁用受阻能力 | v2 Schema 校验真实 API 响应；生产 deny-all 最多 `PACKAGED`；测试注入不进公开 Schema/响应；降级、伪造、错配、过期、依赖/许可路径负例 | Transformer、模板、核心配置、SQL/脚本等尚未盘点；72 项均未取得 E3/E4，真实执行与 Windows 发布仍阻断 |

当前自动化还通过上游源码身份锁定（`datax_v202309` / `9a1f887...` / 1,346 个文件）、
Reader/Writer inventory 重建检查和 `git diff --check`。以上结果都没有连接真实
MySQL/PostgreSQL + 固定 DataX Runtime，也没有
在干净 Windows 11 x64 上运行 Setup，因此发布状态继续是 `BLOCKED`。

### 2.2 2026-08-02 对抗式审查增量

本轮从“用户拿到 Windows 安装包后能否启动、执行、止损、取证和更新”反向审查正式
Compose、Launcher、Worker、凭据撤销、插件认证和发布流水线。结论不是新增的发布
批准：当前 release gate 仍失败关闭；但确认了以下必须在解锁前关闭的实现缺口：

1. Worker 缺少控制面完整性 HMAC secret，正式 Compose 会在建立 dispatcher 前 crash-loop；
   当前切片已补齐最小只读挂载、Launcher 校验和静态闭包测试，仍须完成真实 Compose/
   Windows 验证。
2. 已领取 Execution 的目标独占撤回/过期和紧急凭据撤销已补入持久终止事实、Worker
   控制边界和 Probe fail-closed 收敛；heartbeat 不会在安全终止后续租、空闲队列会扫描
   自然到期，current secret 紧急撤销也会收敛无绑定队列，lease-LOST Probe 会重新打开
   可提交的 Gate，且 retired secret 可在后来发现泄露时单向升级。当前只完成 E1 控制面/
   进程/Probe 测试，真实 DataX 长任务、PostgreSQL 并发与 Windows E3/E4 尚未证明实际
   停止边界。
3. 生产插件认证固定 deny-all，且 E3/E4 证据需要候选先运行而普通路径先要求 E4，形成
   qualification 自举环。ADR-0011 已接受 P→QH→QR→最终 F→hosted promotion 的
   两阶段设计：Phase A QH 只产生私有非 E4 qualification，Phase B 的精确 F 才能派生
   E4 状态，普通用户另须同一 F 的有效 PR；QH/QR、reader、受保护 harness 和真实证据
   尚未实现，不能以测试注入、设置项或自报状态解锁。
4. 自托管 Windows 签名 Runner 的私钥清理、工具链来源和候选身份尚不能建立
   source-to-binary 信任；同一三段版本也没有不可变候选唯一性。
5. “DataX 所有功能”只有长期分级方向，尚无冻结的机器 target-set、许可例外和
   `target-set = certified-set + explicitly-unsupported` 的可计算完成定义。

这些问题的具体状态、关闭条件和影响范围在下列 `FP-P0-004`、`FP-P1-012..016` 中
维护。它们不会被绿色单元测试、候选 root 或 EXE 文件存在自动关闭。

## 3. 状态和关闭规则

| 状态 | 含义 |
|---|---|
| `OPEN_CONFIRMED` | 已由代码/契约对照确认，尚无足以关闭的实现与证据 |
| `IN_PROGRESS` | 有明确修复 PR，但关闭条件尚未全部通过 |
| `ACCEPTED_PLAN` | 产品/架构方向已由 Accepted ADR 裁决，但实现与取证仍未完成 |
| `BLOCKED_DECISION` | 必须由仓库所有者决定产品/许可/外部依赖边界 |
| `BLOCKED_EXTERNAL` | 需要当前开发机不具备的真实 Windows、数据库、签名或组织系统 |
| `NOT_RUN` | 已有验收设计，但没有可复现执行证据 |
| `CLOSED` | 责任契约、实现、负向测试、必需 E3/E4 和证据绑定全部通过 |

不得因为已开始编码就将状态改为 `CLOSED`。单元测试只能关闭 E1 要求；数据
复制、Windows 安装/恢复分别仍需 E3/E4。每个修复 PR 必须回写本文状态、
证据路径、关联需求/测试 ID 和未验证项。

下列“当前证据”保留 2026-08-01 审查时用来确认根因的代码/契约事实；后续候选
修复不删除根因记录，而是更新“状态”并在关联 PR/证据中证明关闭条件。

## 4. P0 问题台账

### FP-P0-001：验收证据未做语义绑定

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | 发布系统可能把“任意被哈希文件 + 自申报 PASS/E4”当成真实验收，属于假成功门禁风险 |
| 当前证据 | 已有外部 expected candidate/commit 参数、environment/oracle/Windows JSON 解析、绑定、oracle 重放拒绝和实际 Setup/Launcher 文件哈希候选；BLOCKED candidate-root/attestation validator 基础已绑定完整文件集、最终 manifest 1.1 和固定 GitHub workflow 身份。Release workflow 已有 hosted root/attest/低层反向验证的 E1 接线，但尚未取得受保护 Windows harness、真实 attestation bundle、干净 VM 场景或完整 E3/E4 外部信任链 |
| 责任边界 | Release/QA 主责；Backend/oracle 提供可验证身份；Security 复核反伪造边界 |
| 影响契约 | `acceptance-manifest.v1.schema.json`、`verification-oracle.v1.schema.json`、environment manifest 契约、`09_`、`12_` |
| 状态 | `IN_PROGRESS`；已有 oracle/environment 结构、跨字段语义、身份绑定、oracle 重放拒绝及只能表达 `BLOCKED` 的候选根/托管 attestor 验证基础；独立负向复核仍证明通用文本证据可形成非发布结构 PASS，且真实 workflow attestation、受保护 Windows harness 与场景 profile 未完成，未关闭 |

关闭条件：

- Oracle artifact 必须先通过 JSON Schema 和独立跨字段语义 validator，再允许 E3/E4 PASS。
- Manifest 必须把每个证据绑定到 `requirement_id + test_id + execution_id +
  environment_id + environment_manifest_sha256 + candidate commit/release digest`。
- Environment manifest 必须有 schema 并被解析校验，不能只校验文件哈希。
- Catalog 必须把每个测试的必需 scenario、方向、模式、失败注入和最小
  Execution/oracle 数量固化为机器 profile；不能用一个成功断言满足“四方向、正常/空源/
  脏行、安装/重启/恢复”等复合验收语义。
- 公开发布必须验证受信 Windows harness/候选根 attestation（绑定仓库、workflow、run、
  commit、最终 release manifest 和真实制品）；在该信任根实现前，`--require-pass` 必须
  无条件失败关闭，自报 `clean_vm=true`、`VALID` 或调用者传入的 expected commit 都不是证明。
- 加入“普通文本伪装 oracle”、“PASSED oracle 来自另一 Execution/环境/候选版本”、
  “候选代码在取证后改变”负向测试。
- 只有受保护 Windows harness/candidate attestation validator 实现且上述校验全部通过后，
  `--require-pass` 才能返回成功；当前必须无条件返回
  `TRUSTED_RELEASE_ATTESTATION_NOT_IMPLEMENTED`。
- `--require-pass` 的 expected commit/release candidate 必须来自受保护 checkout/tag 上下文，
  不能从待验证 manifest 自身读取；oracle artifact/execution 不得跨 entry 重放。
- Windows E4 必须由受保护 runner 实际读取 Setup/Launcher、调用系统 Authenticode 验证、
  执行干净 VM harness 并保存不可替代证据；结构化 JSON、自声明 `VALID` 和文件哈希不能
  单独关闭本问题。
- 非发布校验即使得到结构 `gate_result=PASS`，也必须报告 `release_approved=false`，不得
  用于发布或把 FP-P0-001 标记为关闭。

### FP-P0-002：需求 catalog 会从需求 ID 内误提取测试 ID

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | 验收集合包含权威矩阵未定义的幽灵测试 ID，发布覆盖面不再等于文档声明 |
| 当前证据 | `scripts/acceptance/catalog.py` 在整行上使用无单元格边界的正则；如 `NFR-PERF-001` 会同时产生实际测试 ID 与子串 `PERF-001`，同类误提取共 5 个 |
| 责任边界 | Release/QA |
| 影响契约 | `12_`、`requirements-catalog.v1.json`、acceptance manifest 生成与校验器 |
| 状态 | `CLOSED`（E1）；已按单元格解析并重生成 81 个需求/85 对 catalog/hash，错列、多 ID、重复 test/pair、旧 hash 与旧 schema 失效回归均通过 |

关闭条件：

- 按 Markdown 表格单元格解析，只从“测试 ID”列取值；不得在整行上搜索。
- 测试覆盖 catalog 中不存在 `PERF-001/PERF-002/SEC-001/SEC-002/SEC-003`。
- 重新生成 catalog 和 hash，并确认所有下游 manifest 因旧 hash 而失效。
- 添加需求 ID 内含合法测试前缀子串、表格错列、多测试 ID 和重复对的测试。

### FP-P0-003：恢复 staging 后的普通启动未受 journal 门禁

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | 用户可能在恢复已暂存但未提交时执行普通 Start，随后走全新 `LEGACY` 初始化；部分恢复不能被当成新安装 |
| 当前证据 | `stage_system_restore()` 在 staging 后返回 `RESTORE_STAGED_COMMIT_BLOCKED`；`Action::Start` 在运行 `ensure_runtime_secrets()` 前未检查受认证 restore journal/staging |
| 责任边界 | Windows Launcher/Recovery |
| 影响契约 | ADR-0008、`system-backup.v1.md`、`runtime-generation.v1.schema.json`、Windows 恢复验收 |
| 状态 | `IN_PROGRESS`；`Start/Backup` 已增加初始化/读取前固定恢复根门禁并通过 E1，继续恢复/授权清理、崩溃矩阵和 Windows E4 尚未完成 |

关闭条件：

- 所有可写 Launcher action 首先检测恢复 journal/staging 不变量；存在未完成恢复时
  只允许继续同一恢复或执行 journal 授权的清理。
- 普通 Start/Backup/initialization 稳定失败关闭，不创建秘密、固定卷、安装标识或活动
  代际。
- 断电/强杀后重复启动覆盖 journal 存在、篡改、未知 staging 对象和授权清理。
- 最终在干净 Windows 11 x64 上执行恢复中断 E4 故障注入。

### FP-P0-004：Worker 的 Compose secret 闭包不完整，正式启动会 crash-loop

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | Worker 是唯一可推进 Execution/RecoveryProbe 的组件；若它在 dispatcher 建立前退出，页面、API 和镜像健康均不能形成可执行复制产品 |
| 当前证据 | `build_control_service()` 与 `build_credential_service()` 均在 Worker 启动时读取 `/run/secrets/idempotency_hmac_key`，但旧 Compose/Launcher Worker secret 集合只给数据库密码和 KEK；当前切片将 HMAC 以只读 Docker secret 挂载给 Worker，并让 Launcher 与 Python 测试验证精确最小集合 |
| 责任边界 | Windows Launcher/Compose/Worker |
| 影响契约 | Windows Compose、Launcher secret 校验、`10_` Secret 生命周期、`PRD-FR-WS-001` |
| 状态 | `IN_PROGRESS`；Compose、Launcher 和 E1 secret-closure 测试已通过，尚未用真实 release image/Windows Docker Desktop 证明完整启动 |

关闭条件：

- API/Worker 的每项启动期 secret 依赖都有单一、可审计的 Compose source/target 和 Launcher
  运行时文件类型/长度校验；Worker 不获得 JWT 私钥、公钥或 refresh-token HMAC。
- unit/Compose-rendered contract 测试必须在缺失、额外和错误 target 三种反例失败；Worker
  入口在所需 secret 都存在时达到 heartbeat/reconciler/dispatcher 建立点。
- 在固定 release image 的 Docker Desktop + Windows 11 E4 环境验证首次启动、停止和重启，
  并确认日志不泄露 secret 内容。

## 5. P1 问题台账

### FP-P1-001：恢复/备份尚未真正以活动运行代际为唯一数据源

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | ADR-0008 要求一个指针选择 installation-id、secret 和三个卷，但初始化/备份路径仍默认固定 LEGACY 对象；未来 `FRESH/RESTORE` 将无法被统一启动和备份 |
| 当前证据 | `RuntimeGeneration` 只有 `legacy()` 构造器；`ensure_runtime_secrets()`、`prepare_system_backup()`、卷存在性和 DATA/SECRETS 导出仍使用固定 secret 目录/卷并强制 `ensure_legacy_runtime_generation()` |
| 责任边界 | Windows Launcher/Recovery |
| 影响契约 | ADR-0008、`system-backup.v1.md`、runtime-generation Schema、Compose/Windows E4 |
| 状态 | `OPEN_CONFIRMED` |

关闭条件：建立 `FRESH/RESTORE` 代际构造与对象实现，并使 Start、Stop、Backup、
Restore、diagnostics、uninstall/reinstall 只从已验证的活动指针获取五项运行身份；
在 E1 崩溃测试和 E4 Windows 恢复演练中证明不会拼接不同代际。

### FP-P1-002：`VERIFYING` 期间取消可能被结束事务越过

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | 大表 oracle 正在读取时的取消可长时间保持 `PENDING`，甚至在 oracle 返回后被写成 `SUCCEEDED` |
| 当前证据 | Worker 进入 `VERIFYING` 后同步执行 `_verify()`；结果后只校验 lease，oracle 批量读取无 CancelRequest 检查，成功终态交易未同时拒绝待处理取消 |
| 责任边界 | Worker/Execution state machine |
| 影响契约 | ADR-0004/0006、Execution/CancelRequest 数据约束、OpenAPI、`E2E-CANCEL-001` |
| 状态 | `IN_PROGRESS`；数据库读取、SQLite 摘要/差异扫描、所有终态取消线性化、核验中目标独占撤回/过期和同事务 RecoveryGate 均有 E1 候选并通过 Backend 回归；真实 PostgreSQL 并发和大表 E3 尚未执行 |

关闭条件：oracle 批量边界检查 cancel/fence/目标独占状态；所有非取消终态事务原子拒绝
待处理取消，`SUCCEEDED/PASSED` 还必须原子拒绝已撤回/过期声明或失租；用真实大表 E3 注入“刚进入
VERIFYING/中批/最后一批”取消，且无任何路径进入成功。

### FP-P1-003：API/Worker 责任边界未被 PostgreSQL 权限强制

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | PostgreSQL 是唯一事实源，但 API 与 Worker 拥有当前及未来全表相同 DML；API 缺陷/失守可越过 Worker 状态机改写执行、锁、oracle 和审计事实 |
| 当前证据 | migration `20260731_0013_runtime_database_roles.py` 向两个运行角色一律授予全表 `SELECT/INSERT/UPDATE/DELETE`，并对未来表设同样 default privileges |
| 责任边界 | Database/Security；Backend 提供精确写入用例 |
| 影响契约 | ADR-0002/0006、数据模型、迁移、威胁模型、权限验收 |
| 状态 | `OPEN_CONFIRMED`；API/Worker 已使用独立登录角色且均无 DDL/TEMP 权限，但两者仍对当前和未来全表共享 `SELECT/INSERT/UPDATE/DELETE`；写集矩阵、细粒度授权迁移和 API 越权反例尚未实现 |

关闭条件：生成 API/Worker 表级/列级/存储过程写集矩阵，用迁移撤销全表通配
权限；状态推进、fence、oracle、RecoveryProbe 和审计追加由数据库权限/约束拒绝 API
凭据；迁移正向/回滚及越权反例通过。

### FP-P1-004：容器默认拒绝出口存在绕过路径

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | nft output 拒绝只作用于 egress-guard 共享 netns；PostgreSQL、migrate 和 web 处于普通可 NAT 的 bridge，受害镜像可绕过主机/容器级默认拒绝外联 |
| 当前证据 | `deploy/windows/compose.yaml` 只使 API/Worker 共享 egress-guard netns，`control` 网络未设 `internal: true`；`migrate` 还持有 owner/guard/API/Worker 数据库密密引用 |
| 责任边界 | Deployment/Security |
| 影响契约 | ADR-0005、Compose、egress-guard 契约、威胁模型、Windows 网络 E4 |
| 状态 | `OPEN_CONFIRMED` |

关闭条件：为不需要外联的 control 平面建立内部网络或等效宿主强制；将
migrate 的高权秘密生命期缩到迁移阶段；用真实容器对每个服务执行外联正/负例，并在
Windows Docker Desktop/WSL2 上验证 guard death 和 NAT 绕过均失败。

### FP-P1-005：PRD/UI/API 的用途和 TransferPolicy 可见性契约冲突

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | 对同一授权行为存在多个答案，实现无法客观验收，且可能多披露管理策略或让 Developer 用错方向权限 |
| 当前证据 | PRD 要求 Developer 按获准 `SOURCE_USE/TARGET_USE` 用途读元数据，而 metadata API 没有 `usage` 请求维度，实现以任一用途授权放行；PRD/UI 将 TransferPolicy 管理定义为 Admin-only，API 当前允许项目可读成员查看完整策略 |
| 责任边界 | Product 决定用户语义；API/UI/Backend 同步实现 |
| 影响契约 | PRD 权限矩阵、UI、OpenAPI、数据模型、RBAC 测试 |
| 状态 | `IN_PROGRESS`；PRD 责任语义已同步到 OpenAPI/Backend/UI 并通过契约、类型和构建检查，真浏览器四角色/两用途验收尚未执行 |

责任来源和修复方向：PRD 对用户价值与权限承诺负责，因此当前默认不放宽。
OpenAPI 应增加明确用途并让 Backend 只按所请用途授权；TransferPolicy 列表/详情应限 Admin，
或由 Product 通过正式 PRD/ADR 决定并定义最小披露摘要。需要四角色和两用途的正/负向
契约与浏览器测试。

### FP-P1-006：FQDN/`VERIFY_FULL` DataX 安全执行仍不可用

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | 企业数据库常使用 DNS 和 TLS 主机身份；当前 DataX 主链路只允许唯一 `EXACT_IP`，并拒绝 `VERIFY_FULL`，与 V1 的真实 TLS 验收合同不一致 |
| 当前证据 | Worker `_assert_datax_endpoint_pinning()` 显式拒绝 FQDN 及 `VERIFY_FULL`；部署文档将其标为 fail-closed 工程缺口 |
| 责任边界 | Runtime/Network/Security |
| 影响契约 | ADR-0005、EndpointPolicy/egress-guard、Runtime manifest、MySQL TLS E3 |
| 状态 | `OPEN_CONFIRMED` |

关闭条件：在专属 netns 或驱动层实现“selected IP 建连 + 原 FQDN TLS 身份验证”；
覆盖 multi-A/AAAA、DNS rebinding、SAN/CN 不匹配、未知 CA、租约过期/撤销和 control 子网绕过；
最后用固定 Connector/J 9.7.0 真实握手证据关闭，不接受 URL 字符串断言。

### FP-P1-007：前端和浏览器闭环没有可计算测试证据

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | 用户最终操作的是浏览器页面；仅 typecheck/build 不能证明四角色、失败态、取消和恢复链路可用 |
| 当前证据 | `frontend/package.json` 无 test/coverage/E2E 命令，无前端 `*.test.*/*.spec.*`；CI 前端只运行 typecheck/build；UI 文档的 `ACC-UI-001..018` 未进入当前机器验收 catalog；`09_` 要求 Backend/前端整体行覆盖≥80%、核心≥90%，但现有 CI/manifest 只统计需求条目覆盖，不产出代码覆盖率报告 |
| 责任边界 | Frontend/QA |
| 影响契约 | UI 文档、`09_`、`12_`、acceptance catalog/manifest、CI |
| 状态 | `OPEN_CONFIRMED` |

关闭条件：先决定 `ACC-UI-*` 与权威需求 catalog 的关联方式，再接入组件/行为测试、
真浏览器 E2E、可访问性和覆盖率；四角色完成授权→发布→执行→取消/失败→恢复
链路，Backend/前端整体行覆盖≥80%、核心模块≥90%，并保留机器报告和关键截图。

### FP-P1-008：外部 WORM 信任锚与“只靠本机”目标存在待决策张力

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | 同一台电脑的管理员可同时改动数据库记录和本机链头；没有独立信任锚就不能承诺抗本机/数据库管理员篡改 |
| 当前证据 | 当前只实现同库 `DXAUDITv1` 链；部署/威胁文档要求数据库外签名 checkpoint/WORM，但没有选定可交付的本地/外部介质、Provider 和责任人 |
| 责任边界 | Product Owner/Security/Operations |
| 影响契约 | NFR-AUD-001、威胁模型、部署、备份恢复、Windows 验收 |
| 状态 | `BLOCKED_DECISION` |

所有者必须三选一并通过 ADR 固化：

1. 使用外部对象锁定/WORM 服务，接受这一项外部服务依赖。
2. 使用移动介质/离线签名锚点，定义断开、丢失和人工转移流程。
3. 降低 V1 承诺，只声明同库链可检测普通应用路径篡改，不承诺抵抗本机管理员。

### FP-P1-009：真实 DataX/Windows 发布闭环尚未取证

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | 当前候选无法回答“在目标 Windows 电脑上能否安装、真复制、失败恢复并稳定重启” |
| 当前证据 | 四方向 E3、完整恢复、签名 Setup/Launcher 与干净 Windows 11 x64 E4 均为 `NOT_RUN/BLOCKED` |
| 责任边界 | Runtime/Backend/QA/Windows Release |
| 影响契约 | `09_`、`10_`、acceptance manifest、`E2E-DX-001..004`、`E2E-WIN-001/002` |
| 状态 | `BLOCKED_EXTERNAL` |

关闭条件：只能由当次候选版本的真实 MySQL 8/PostgreSQL 15/DataX 产物和干净
Windows 11 x64 证据包关闭；开发机单元测试、镜像存在、Windows runner 编译和 EXE
文件存在不能关闭。

### FP-P1-010：候选插件被 API/UI 命名为“已认证”但没有 E3/E4 证据

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | `CERTIFIED` 如果只代表 JAR 和 Runtime 健康，会把“可加载”误写成“真实系统与 Windows 已稳定可用”，普通用户可能执行未经认证的能力 |
| 当前证据 | v1 保留作为历史 Schema；对外 API 已改为 `listPluginCapabilities` + `plugin-manifest.v2`，生产默认 deny-all、四插件最多 `PACKAGED`，测试注入不会序列化为 E4。普通创建/恢复 rerun/Worker claim/start 四个检查点与 UI 均已失败关闭；四方向真实 E3 与 Windows E4 仍为 `NOT_RUN/BLOCKED` |
| 责任边界 | Product/Runtime/API/Frontend/Windows Release |
| 影响契约 | ADR-0009、Plugin Manifest、OpenAPI、JobSpec、Runtime attestation、UI、acceptance manifest |
| 状态 | `IN_PROGRESS`；契约/代码/E1 负例已修复，真实 E3/E4 取证未完成 |

关闭条件：先生成覆盖完整上游的机器能力 inventory，并区分 `SOURCE_PRESENT / BUILD_VERIFIED /
PACKAGED / CONTRACTED / E3_CERTIFIED / WINDOWS_E4_CERTIFIED / BLOCKED`；API 状态必须由固定
候选的受信证据派生，只有 `WINDOWS_E4_CERTIFIED` 且同一精确最终 F 存在有效 PR 才能进入
普通用户可执行选择器。开发/测试候选如需运行，必须以明确的非发布模式展示且不能把状态
序列化为 `CERTIFIED`；补充伪造状态、证据失效、降级和 UI 禁用负例。

### FP-P1-011：恢复门禁曾在失败终态后的第二事务补写

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | Execution 已进入失败终态且目标锁要求恢复后，如果进程在第二事务创建 `RecoveryGate` 前崩溃，Operator 会看见不可再次执行的锁，却没有可达的处置入口 |
| 当前证据 | 旧 Executor/取消/Reconciler 路径在终态事务提交后调用 `ensure_terminal_gate`；该调用失败不会回滚已经提交的 Execution 与 TargetCopyLock |
| 责任边界 | Execution state machine/Recovery/Worker reconciler |
| 影响契约 | ADR-0006、RecoveryGate 数据约束、失败恢复与 lease-LOST 验收 |
| 状态 | `IN_PROGRESS`；正常 `FAILED/TIMED_OUT/CANCELED/LOST` 路径已在同一事务创建唯一 gate，gate 写失败整体回滚，旧 helper 只保留为幂等历史修复入口；真实 PostgreSQL crash/concurrency E3 尚未执行 |

关闭条件：所有可产生 `RECOVERY_REQUIRED` 的正常终态路径必须在同一数据库事务创建唯一
`RecoveryGate`；gate 写失败时 Execution、Attempt、取消请求和 TargetCopyLock 一起回滚；历史
缺失 gate 只能由显式对账修复，不能继续作为正常路径正确性依赖。E1 覆盖四终态、写失败、
提交后进程崩溃和 lease-LOST；再以真实 PostgreSQL 进程终止/并发 E3 证明不存在可观察间隙。

### FP-P1-012：目标外部独占撤回/过期不会立即终止已运行 DataX

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | Operator/DBA 已报告目标冻结窗口失效后，产品仍可能继续 `insert-only` 写入目标；最终拒绝 `SUCCEEDED` 不能撤销已经扩大的污染和恢复成本 |
| 当前证据 | `20260802_0015` 已新增可回滚 `work_termination_requests`、生命周期/原因/双部分唯一约束和扫描索引；撤回与到期生成目标独占终止事实，reconciler 每轮固化空闲队列到期，Execution Worker 在 admission、每次预检 DNS/JDBC/schema/fingerprint/count 有界 I/O（含 connector probe 查询间隙）前后、敏感 Job 文件写入前、heartbeat、DataX process tick、oracle 控制边界和 process helper 的 Popen 紧前消费；heartbeat ACK 后不续租，`TERMINATE` 复用进程组 SIGTERM→SIGKILL，当前 fence 原子写 `FAILED`、恢复门禁并完成请求。控制面早期/空闲到期、各 admission 边界、取消竞态、heartbeat 及无 Popen 安全终止 E1 已通过。数据库提交与实际连接/Popen 仍无单一原子提交，最后检查只能阻止已观察到的请求；尚未在真实 DataX/PostgreSQL 长任务上取证 |
| 责任边界 | Execution state machine/Worker/Recovery |
| 影响契约 | `03_`、`04_`、`09_` 的目标独占撤回语义；`PRD-FR-RUN-001`、`PRD-BR-005` |
| 状态 | `IN_PROGRESS`；实现、迁移与 E1 回归已落地，E2 PostgreSQL 并发和 E3 真实 DataX 终止边界未执行 |

关闭条件：E2 在真实 PostgreSQL 验证撤回/claim/terminal/heartbeat 的锁序、无死锁、无漏
请求和无撤回后成功；E3 对真实 DataX 长任务覆盖早/中/晚撤回、正常退出、用户取消和旧
fence 竞态，并测量从撤回提交到实际连接/进程停止的剩余窗口。若没有可接受的启动/egress
收敛边界，则该 P1 不能关闭。当前 STARTING 可保留 `data_effect=NONE`，已运行或已启动 oracle
必为 `UNKNOWN/INCONCLUSIVE + RECOVERY_REQUIRED`；不得把该 E1 结果描述为真实数据库停止证明。

### FP-P1-013：紧急 CredentialSecret 撤销不终止活动 Execution/RecoveryProbe

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | 已确认泄露的密码在撤销提交后仍可由已解密 Worker 连接数据库，RecoveryProbe 甚至可能发布 `EMPTY/VERIFIED`，与紧急撤销的安全承诺相反 |
| 当前证据 | `REVOKED/COMPROMISED` 现在与 secret 状态/Datasource 禁用同一事务为已绑定的非终态 Execution 与 RecoveryProbe 建立每 secret 独立终止事实；若 secret 仍是 current，同时覆盖不可变引用其 DatasourceRevision 的无绑定队列，避免 `RESERVED`/恢复门禁永久占用。直接退役 current secret 返回 `CREDENTIAL_STATUS_CONFLICT`；只有已切换 current 的历史 `RETIRED` 版本不建立请求但可单向升级到紧急终态。work row 与 terminal write 序列化，Worker 内部失败也会优先收敛系统终止。每端 Worker 解密改为 fresh short `FOR UPDATE` 状态锁，事务在 yield 明文前释放；端间与后续外部 I/O（含 probe 查询间隙）复检终止事实，因此状态先提交拒绝新解密，解密先完成也不能继续新连接；`bytearray` 清零只是尽力而为，不能声称清除 Python/驱动/JVM/子进程全部副本。Probe 的 heartbeat、DNS/连接/schema/count 边界与 `complete_probe()` 均检查该事实，晚到 EMPTY 收敛为 `FAILED/INCONCLUSIVE + Gate=REJECTED`，lease-LOST 也重开 Gate。安全回归、控制面与恢复回归均为 E1；真实已解密密码、JDBC 阻塞、PostgreSQL 并发和实际 DataX 仍未取证 |
| 责任边界 | Credential/Execution/Recovery/Worker |
| 影响契约 | `03_`、`04_`、`10_` 凭据紧急撤销合同；`NFR-SEC-001` |
| 状态 | `IN_PROGRESS`；ORM/迁移/Worker 消费和 E1 反例已落地，E2/E3 仍未执行 |

关闭条件：E2 在真实 PostgreSQL 验证 secret revoke（含 `RETIRED→COMPROMISED`）与
Worker claim/terminal/heartbeat/数据源更新并发时无死锁、不漏请求且无撤销后成功；E3 使用
真实 DataX 和真实 RecoveryProbe 覆盖解密前/后、RUNNING、VERIFYING、probe completion
及 `RETIRED` 对照，并测量从状态提交到外部连接停止的边界。阻塞 JDBC 只能以超时/lease-
LOST 保守收敛，不能以 E1 mock 或请求状态误称“即时停止”。

### FP-P1-014：插件认证的 qualification 自举环使真实候选无法取证

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | 普通创建/领取/启动正确地要求最终 E4 证据与公开晋级，但同一候选需要先运行 E3/E4 才能产生这些事实；生产 deny-all 与测试注入之间没有安全的 qualification 通道 |
| 当前证据 | `build_control_service()` 不加载受信证明源，正式默认拒绝全部插件；唯一可放行 source 是不可由 settings/env 构造的 `TEST_INJECTION`。ADR-0011 已接受不可变 payload P、受保护短期 QH、独立 QR、无 QH 的最终 F Phase B 和 hosted promotion 的分离链，以避免把资格回写 Worker/Setup 后改变 digest/SHA；当前没有其中任一 schema、reader、harness、签名 authority 或真实证据 |
| 责任边界 | Runtime/Release/QA/Security |
| 影响契约 | ADR-0009、plugin manifest、release candidate/attestation、E3/E4 验收 |
| 状态 | `ACCEPTED_PLAN`；架构自举环已由 ADR-0011 裁决，实际 production reader、候选资源、负向测试与真实 E3/E4 仍未实现 |

关闭条件：按 ADR-0011 新增并审查 P/QH/QR/final candidate-root 契约、固定 HQA/RQA
keyring、canonical parser、QH private source、QR production reader 和 API/Worker 四检查点；
标准 Compose/Setup/公开 artifact 必须拒绝 QH，普通 API/UI 不得通过 settings/env/测试
source/自报 JSON 解锁。先以受保护 Phase A 对精确 P 取得真实 E3/私有 Windows
harness/payload qualification（非 E4），再经独立 RQA 签发 QR；随后构建精确 F，在无 QH
的标准路径完成 Phase B Windows E4，才可派生 `WINDOWS_E4_CERTIFIED`。该状态还必须与
同一 F 的 hosted candidate-root/provenance、完整发布 validator 和有效 PR 共同成立，才可
普通用户执行或公开发布。任一 Windows、数据库、签名、OIDC 或负向证据缺失时保持 BLOCKED。

### FP-P1-015：Windows 发布链无法证明源码到签名二进制，并可能遗留签名能力

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | 被攻陷或硬中断的自托管 Runner 可以遗留可用私钥、劫持 PATH 工具链或替换产物后重新计算 handoff；托管 attestor 只能证明它收到的字节，不能证明字节来自审核源码 |
| 当前证据 | 签名私钥先导入长期 Runner 用户证书库，`always()` 只能清理当前 run；Cargo/NSIS/signtool 通过 PATH 解析且不锁工具 hash/签名/父目录 ACL；candidate root 未包含 source-to-binary provenance；同一 semver 允许多个 candidate 且 Setup 内没有 candidate ID。ADR-0011 的 HQA/RQA/QR 不能替代 unsigned build provenance、不可导出 Authenticode 或 hosted candidate-root；这些角色必须相互独立 |
| 责任边界 | Windows Release/Security/QA |
| 影响契约 | release workflow、installer manifest、candidate-root、Windows E4/签名验收 |
| 状态 | `OPEN_CONFIRMED` |

关闭条件：使用一次性受控 builder 或不可导出 HSM/远程签名，建立受信 unsigned build
provenance → 非导出签名 → 独立 E4 harness → ADR-0011 精确 P/QR/F → ADR-0010 hosted
candidate-root 的单向链；工具使用绝对受保护路径、版本、hash/Authenticode 和净化环境。
promotion ledger 强制一个 semver 只绑定一个不可变 candidate，安装器持久化 candidate ID
并拒绝同版本不同候选覆盖。对 PATH shim、`RUSTC_WRAPPER`、post-build replacement、
硬杀 runner、QR 替换、同 semver 重放和 self-hosted provenance 混淆的负例必须失败。

### FP-P1-016：“DataX 所有功能”没有机器 target-set 和完成判定

| 字段 | 内容 |
|---|---|
| 第一性原理影响 | “源码存在”或 72 个 Reader/Writer 的 `SOURCE_PRESENT` 都不能证实所有可合法模块可在 Windows 安全运行；开放集合会让最终交付永远无法证伪或验收 |
| 当前证据 | V1 API/JobSpec/UI 和 Worker Runtime 只允许四个 MySQL/PostgreSQL 插件；当前 inventory 只盘点 72 Reader/Writer，Transformer、模板、核心配置和 SQL/脚本能力未形成 canonical target-set；requirements catalog 没有 target-set 与 certified-set 的覆盖 oracle |
| 责任边界 | Product/Runtime/Security/Legal/QA |
| 影响契约 | ADR-0009、PRD、Plugin Manifest、JobSpec/OpenAPI、许可清单、E3/E4 matrix |
| 状态 | `OPEN_CONFIRMED` |

关闭条件：冻结版本化 canonical capability catalog，逐项记录上游模块/功能、版本、许可、
驱动、风险类别、永久 `UNSUPPORTED` 例外、可执行条件和 E3/E4 测试 ID。只有
`target_set - explicitly_unsupported == WINDOWS_E4_CERTIFIED_set`，且每项都由其精确最终 F 的
有效 PR 覆盖时，才允许“所有功能”声明；在此之前只按第 7 节四层口径陈述 V1 和已认证扩展。

## 6. 冲突记录与责任来源

| 冲突 ID | 主题 | 当前冲突 | 责任来源 | 实施状态 |
|---|---|---|---|---|
| `FP-CON-001` | 元数据用途 | PRD 要求分别检查 `SOURCE_USE/TARGET_USE`；旧 OpenAPI/实现无用途维度 | PRD | `IN_PROGRESS`；契约/实现候选已按精确用途收敛，待浏览器验收 |
| `FP-CON-002` | TransferPolicy 披露 | PRD/UI 为 Admin-only；旧 API/实现对项目可读成员返回完整对象 | PRD；如改承诺须先改 PRD | `IN_PROGRESS`；列表/详情候选已限 Admin，待浏览器验收 |
| `FP-CON-003` | 新装运行代际 | ADR-0008 要求首次初始化生成 `FRESH` 随机对象集；Launcher 目前生成固定 `LEGACY` 集合 | ADR-0008 | `BLOCKED` |
| `FP-CON-004` | 备份对象选择 | ADR-0008 要求活动指针是唯一来源；backup 仍强制固定 LEGACY secret/卷 | ADR-0008 | `BLOCKED` |
| `FP-CON-005` | 验收语义 | `09_` 要求 E3/E4 PASS 引用真实 PASSED oracle 及环境；旧 validator 只验字段和哈希 | `09_`/oracle 契约 | `IN_PROGRESS`；结构/部分绑定已实现，自申报、重放和可信 runner 仍阻塞 |
| `FP-CON-006` | 全部 DataX 能力 | 最新目标希望“包含 DataX 所有功能”；V1 只允许 MySQL/PostgreSQL 四插件和受控一次性复制 | ADR-0009 已接受“完整上游随包 + 按能力单向分级认证”；V1 范围不变 | `ACCEPTED_PLAN` |
| `FP-CON-007` | 插件认证状态 | ADR-0009/0011 要求状态只能来自精确最终 F 的 Phase-B E4，普通用户还须同一 F 的有效 PR | ADR-0009、ADR-0011 | `IN_PROGRESS`；72 个 Reader/Writer `SOURCE_PRESENT` inventory、v2 Manifest/API/UI 与普通创建/恢复 rerun/Worker claim/start 四检查点已落地。`WINDOWS_E4_CERTIFIED` 已与 `evidence.release_promotion_ref` 分离，缺失时显式 `RELEASE_PROMOTION_REQUIRED` 并继续拒绝；该字段当前仍是不透明引用，尚无受信 reader、结构化 F/candidate-root/签名绑定或真实 E3/E4，production 默认继续全部拒绝 |

## 7. DataX “完整”的四层口径

未来状态页、README、发布页和验收报告必须分别使用以下口径，不得只写
“已支持 DataX”：

| 层级 | 定义 | 当前状态 | 能声明什么 |
|---|---|---|---|
| 上游源码镜像 | 指定 tag/commit 的完整源码树、NOTICE/许可与源文件哈希清单已入库 | 已导入 `datax_v202309` / `9a1f887...`，1,346 个上游文件；其中根 POM 的 72 个 Reader/Writer 已生成 `SOURCE_PRESENT` inventory | 只能声明源码镜像完整且可追溯；不能把 Reader/Writer inventory 当成 Transformer/模板/SQL 等全部功能盘点 |
| 发布 Runtime | 经可重现构建、锁定依赖/驱动/哈希并实际装入 Worker 镜像的插件 | 当前打包四个业务插件 `mysqlreader/mysqlwriter/postgresqlreader/postgresqlwriter`，另有不向用户开放的内部自检插件 `streamreader/streamwriter` | 只能声明四个业务插件和两个内部自检插件已进入候选 Runtime，不能据此声明产品认证通过 |
| 产品开放能力 | JobSpec/OpenAPI/UI 允许选择且有参数白名单、权限、secret 和网络控制 | V1 只设计 MySQL 8/PostgreSQL 15 一次性单表复制 | 只能声明契约预期能力，不能声明 E3 已通过 |
| 产品认证能力 | 真实外部系统、固定 Runtime、产品全链路和独立 oracle 达到要求证据等级 | 四方向均 `NOT_RUN/BLOCKED` | 目前不能声明任何 Reader/Writer 组合已通过产品认证 |

### 7.1 “DataX 所有功能”的已接受实施口径

仓库所有者已通过 [ADR-0009](adr/0009-DataX完整上游与认证能力分级.md) 接受
“完整上游随包与能力分级认证”的长期目标。该决策明确 V1 仍先闭合 MySQL/PostgreSQL
四插件；每个其他插件只能沿 `SOURCE_PRESENT → BUILD_VERIFIED → PACKAGED → CONTRACTED →
E3_CERTIFIED → WINDOWS_E4_CERTIFIED` 单向推进；未到最后一级或没有同一精确最终 F 的有效 PR
均不得出现在普通用户的可执行选择器中。“所有功能”至少可能包括：

- 上游全部 Reader/Writer 插件；
- 自由 `querySql/preSql/postSql`、Transformer 和用户自定义 DataX JSON；
- 依赖 Oracle/SQL Server/HDFS/OSS/ODPS/MongoDB 等外部系统和部分专有驱动；
- 上游已过时、实验性或许可形态不同的模块。

这些能力会改变数据外传、任意 SQL、凭据、网络出口、许可和 oracle 边界。ADR-0009
允许建立 inventory 和逐族认证，但任意 SQL、脚本 Transformer、上传插件等高风险能力仍需
各自的新 ADR，并同步 PRD/威胁模型/OpenAPI/Plugin Manifest/测试/部署；不得通过
“已在源码树中”直接开放。

已接受的交付顺序分为两个里程碑：

1. **可稳定交付的 V1**：先将现有 MySQL/PostgreSQL 安全一次性复制闭环做到
   P0/P1=0、E3/E4 PASS，交付签名 Windows Setup。
2. **分批认证的 DataX 扩展版**：从完整上游模块清单生成机器可读认证
   catalog，每个插件单独记录构建、驱动/许可、参数白名单、secret、出口、类型、
   oracle、真实外部环境和 Windows E4 状态。

插件 inventory 和分级契约可以立即开始，但在必需外部环境、可合法再分发驱动或安全
oracle 不可用时，对应插件状态必须为 `BLOCKED`；被永久阻断的模块必须在“完整
DataX 功能”声明中显式列出。不承诺一次性假实现“100% 上游功能”。

## 8. 分阶段开发与 Definition of Done

下列阶段为当前缺陷的修复优先顺序；不改变 `06_Codex开发执行计划.md` 中的
产品阶段，实施 PR 应将两者同时关联。不得跳过前一阶段的安全门禁后声称后续阶段
完成。

### Repair 0：重建可信验收基础

**输入**：`FP-P0-001/002`。

**DoD**：

- Catalog 只包含权威矩阵明确定义的测试 ID，无幽灵/重复条目。
- Manifest/environment/oracle 以 schema + 语义 validator 联合验证，且证据绑定需求、测试、
  Execution、候选版本和环境。
- 所有伪造、错绑、篡改和降级反例均失败；证据脚本自身测试通过。
- 旧 catalog/hash/manifest 全部失效，只保留不可改写的历史记录，不可复用为新候选 PASS。

### Repair 1：先关闭失败安全 P0/P1

**输入**：`FP-P0-003`、`FP-P1-001/002`。

**DoD**：

- 恢复 journal/staging 存在时，所有普通可写 action 失败关闭；只有受认证继续/清理可用。
- `FRESH/LEGACY/RESTORE` 代际的启动、备份、恢复和诊断都从单一活动指针读取。
- `VERIFYING` 全程可取消，且最终成功写入同时检查 cancel/fence/目标独占状态。
- 单元/数据库/崩溃测试通过；实际恢复和大表取消仍如实标为 E3/E4 待验。

### Repair 2：强制责任和网络边界

**输入**：`FP-P1-003/004/005/006`。

**DoD**：

- 细粒度数据库权限让 API 凭据无法直接推进 Worker 状态、修改 oracle/fence 或改写
  审计链。
- control 平面不能通过 Docker NAT 绕过默认拒绝；migrate 秘密按最短生命周期使用。
- PRD/UI/OpenAPI/Backend 对元数据用途和 TransferPolicy 可见性只有一个答案，并通过四
  角色负向测试。
- FQDN + selected-IP + TLS 原主机身份验证的驱动/网络方案通过真实 MySQL 握手、
  DNS rebinding 和撤策反例。

### Repair 3：完成可操作的 V1 真实纵向链路

**输入**：前述 P0/P1 已关闭，真实 MySQL 8/PostgreSQL 15 和固定 Runtime 可用。

**DoD**：

- 先完成 MySQL→PostgreSQL 黄金路径，再完成其余三方向；全表/选列、类型、NULL、
  Unicode、Decimal、时区、重复行和 0 行数据由独立 oracle 核验。
- 目标非空、源变化、声明过期/撤回、部分写入、超时、取消、Worker 失联和恢复后
  再次执行的真实负向矩阵通过。
- 前端接入组件/行为、浏览器 E2E、可访问性和覆盖率门禁；四角色完成真实主流程。
- 四方向及需求/失败矩阵证据达到 E3，且与当次候选环境/代码精确绑定。

### Repair 4：闭合系统恢复与审计

**输入**：`FP-P1-008`、ADR-0008，以及 Repair 1 的恢复安全不变量。

**DoD**：

- 依 ADR-0008 完成新空 PostgreSQL 卷、真实 `pg_restore`、数据/审计链/日志/密钥证据重算、
  随机 `RESTORE` 代际和提交后 reconcile。
- 中断只能继续同一包对/秘密/journal 或执行 journal 限定清理，不扫描名称前缀批量删除。
- 按所有者 ADR 实现外部 WORM、离线锚定或降低后的审计承诺，不再保持互相矛盾的
  本机产品定义。
- 干净 Windows 异机恢复证明 RPO≤24h、RTO≤4h，数据、任务、Execution、日志和审计完整。

### Repair 5：生成和验收 Windows 发布产物

**输入**：Repair 0–4 达到所需证据等级；Discovery、许可、签名和安全门禁满足。

**DoD**：

- 生成已签名 `DataX-Enterprise-Studio-Setup-<version>-x64.exe` 和已签名 `launcher.exe`，
  发布 SHA-256、时间戳、证书指纹、SBOM、第三方许可和五镜像 digest 完整。
- 干净 Windows 11 x64 覆盖安装/卸载/重装、桌面快捷方式、Docker/WSL/virtualization 失败、
  幂等启动、仅 loopback、LAN 拒绝、睡眠/重启/Docker/WSL 对账和异机备份恢复。
- 同一 Windows 候选完成四方向真实 DataX E3，`E2E-WIN-001/002` 均达 E4。
- Acceptance manifest 所有 V1-MUST 为 PASS，P0/P1=0，Critical/High=0；Discovery Gate 及
  外部人工复核通过。
- 按 ADR-0011 先固定 payload P，在受保护 Phase A 使用短期 QH 完成真实 E3/私有 Windows
  harness/payload qualification（非 E4），由独立 RQA 签发 detached QR；QH 不进入普通
  Compose、Setup、公开候选、Plugin Manifest 或普通用户状态。
- QR 进入精确最终 F 后，在无 QH 的标准 Launcher/Compose 上完成 Phase B Windows E4；只有
  此时才可派生 `WINDOWS_E4_CERTIFIED`。最终 candidate-root 绑定 P、QR、F 和全部证据，
  ADR-0010 hosted attestor/provenance 与发布 validator 形成同一 F 的有效 PR 后才创建公开
  release。当前该链只有 Accepted ADR，仍为 BLOCKED。

### Repair 6：依 ADR-0009 执行 DataX 插件分级扩展

**输入**：ADR-0009 已 Accepted；先生成完整机器 inventory，每个插件在推进到 E3/E4
前仍需对应的真实外部验收环境和合法依赖。

**每个插件的 DoD**：

- 上游 module/commit/文件 hash、构建补丁、依赖、许可、驱动来源和 SBOM 可追溯。
- Plugin Manifest/JobSpec/OpenAPI/UI 只暴露白名单参数；secret、SQL、路径和网络出口分类完成。
- Reader/Writer 配对、类型兼容、空值/编码/时区/重复、失败/取消/部分写入和独立 oracle 有真实 E3。
- 候选 Runtime 按 digest 打包，并在 Windows E4 重新验证安装、资源、网络、备份和恢复。
- 没有真实外部系统、合法可分发驱动或可实现 oracle 时标记 `BLOCKED/UNSUPPORTED`，不用
  Mock 换成“已支持”。

## 9. 横向门禁与责任人

| 门禁 | 责任人/角色 | 必需证据 | 当前状态 |
|---|---|---|---|
| Discovery Gate | Product Owner/Product Research | 8–12 访谈、20 真实脱敏任务样本、3 设计伙伴合计≥10 真实任务，含反证和签署 | `NOT_EVIDENCED` |
| P0/P1 Gate | 各问题 owner + QA/Security | 本文每项关闭条件和负向证据 | `BLOCKED` |
| DataX E3 | Runtime/Backend/Worker/QA | 四方向、全表/选列、失败矩阵、oracle 和候选绑定 | `NOT_RUN/BLOCKED` |
| Browser/UI | Frontend/QA/Product | 四角色真浏览器主链路、失败/权限/可访问性、覆盖率 | `NOT_RUN/BLOCKED` |
| Recovery | Windows/Database/Security/QA | 干净异机 `pg_restore`、原子代际、崩溃注入、RPO/RTO | `BLOCKED_NOT_IMPLEMENTED` |
| Audit anchor | Product Owner/Security/Operations | 已选定 ADR、签名 keyring、数据库外锚定/降低后承诺的证据 | `BLOCKED_DECISION` |
| Windows E4 | Windows Release/QA/Security | 签名 Setup/Launcher、干净 Win11 x64、运行/重启/卸载/备份恢复、LAN 负例 | `NOT_RUN/BLOCKED` |
| 插件资格与发布晋级 | Runtime/Release/QA/Security | ADR-0011 的不可变 P、受保护 QH、独立 QR、无 QH 的精确 F Phase B、ADR-0010 hosted candidate-root/provenance 与负向证据 | `ACCEPTED_PLAN/BLOCKED`；无 schema、reader、签名 authority 或真实外部证据 |
| License | Repository Owner/Legal | 根 `LICENSE`、依赖/驱动许可清单和发布告知 | `OWNER_BLOCKED` |
| Code signing | Repository Owner/Windows Release | 受信证书、可信时间戳、证书轮换/失效负例 | `OWNER_BLOCKED` |
| DataX 全插件扩展 | Product Owner/Runtime/Security/Legal/QA | ADR-0009、机器插件 inventory、真实外部系统、驱动/许可、每插件 E3/E4 | `IN_PROGRESS/SOURCE_PRESENT`；仅 72 个 Reader/Writer 完成第一层盘点，其余原生功能未盘点，所有插件未认证 |

## 10. 立即开发顺序和 PR 切片

开发只能按可独立审查、回滚和验证的纵向切片推进：

1. **PR-A：验收基础**——修复 catalog 单元格解析、oracle/environment 语义校验和候选
   绑定，只修证据系统。
2. **PR-B：恢复 journal 全局门禁**——普通 Start/Backup/初始化受阻，覆盖崩溃和
   负向测试。
3. **PR-C：运行代际统一**——实现 `FRESH/RESTORE` 对象与所有 Launcher 消费者，不同时
   开放覆盖升级。
4. **PR-D：核验取消与终态原子性**——增加批次取消、终态门禁和真实大表测试
   入口。
5. **PR-E：数据库职责权限**——用可回滚迁移缩小 API/Worker 权限并添加越权负例。
6. **PR-F：容器出口边界**——收紧 control/migrate/web/postgres 网络并完成组件级外联证据。
7. **PR-G：权限契约统一**——先修 PRD/OpenAPI/Schema，再改 Backend/UI 与四角色测试。
8. **PR-H：FQDN/TLS 真实执行**——完成 selected-IP + 原主机身份方案和真实驱动
   E3。
9. **PR-I：前端可计算验收**——先进入 UI 验收 catalog，再加行为/浏览器/可访问性/
   覆盖率。
10. **PR-J0：资格链基础**——先只实现 ADR-0011 的 P/QH/QR 契约、固定 keyring、
    production deny-all reader、受保护 override 与负向测试；不生成 release，不把任何
    Windows/签名/OIDC 结果写成 PASS。
11. **PR-J：真实 E3 与恢复**——仅在 J0 的 Phase A 通道和前述安全门禁通过后，按
    MySQL→PostgreSQL 黄金路径、其余三方向、异机 restore 分开提交真实证据。
12. **PR-K：Windows 发布候选**——先由独立 RQA 签发 QR，再构建精确 F、在无 QH 的
    Phase B 运行 E4 并派生 E4 状态、生成最终 candidate root 并经 hosted attestor 验证；
    只有全部发布门禁形成同一 F 的有效 PR 后才签名、公开发布。

每个 PR 只处理一个目的，不得在同一 PR 中同时“修恢复 + 增插件 + 改 UI + 发布”。
当前 GitHub `#1` 是用于同步和独立审查全量工程候选的集成 Draft，包含历史上多个纵向
切片，不能以当前形态合并；后续进入可合并阶段前，必须按上表拆成可独立审查、回滚和
验证的 PR，或由仓库所有者通过新的 Accepted ADR 明确改变该治理规则。集成 Draft 通过
CI 只说明组合候选未发现对应自动化回归，不豁免任何切片证据与发布门禁。

## 11. 本文的完成定义

本文建立后，它只表示“已知问题有权威台账和可执行顺序”，不表示问题已修复。
台账只有在下列条件全部满足时才可标记完成：

- 本文所有 P0/P1 为 `CLOSED`，且无新增未处理 P0/P1。
- 所有冲突已由关注点责任文档裁决，派生契约、实现与测试已同步。
- Discovery、DataX E3、Browser/UI、Recovery、Audit anchor、Windows E4、License 和签名
  门禁通过。
- 发布候选与证据精确绑定，且由非开发者复核。
- 如果最终承诺包含扩展 DataX 插件，其机器认证 catalog 不存在 `UNKNOWN`，
  每个对外开放能力均有真实环境和所需 E3/E4 证据。

在此之前，可以继续工程候选开发，但不得使用“已完成”、“稳定可发布”、“包含
DataX 所有功能”或“生产就绪”等表述。
