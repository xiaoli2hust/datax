# 文档总览与决策基线

| 项 | 值 |
|---|---|
| 文档版本 | 1.4 |
| 基线日期 | 2026-08-02 |
| 状态 | V1.2 Windows 本地工作站工程候选；Discovery Gate 未取证，试点/发布 `BLOCKED` |
| 适用对象 | 产品、设计、研发、测试、运维、安全、编码 Agent |
| 生产状态 | 未批准；需通过 `09_测试与验收标准.md` 的发布门禁 |

> 当前证据边界：后端/Worker、数据源/任务/执行前端页面与正式 API 适配、Launcher/Setup
> 骨架、API/Worker 独立数据库登录、敏感 tmpfs/资源上限、DATA/SECRETS 分离加密导出和
> 双包认证 journal/空 staging、GitHub 安全工作流属于工程候选。旧式完整对象集合的
> `LEGACY` 活动指针原子提交与 Compose 整组消费已有候选代码；恢复仍只达到 E1 staging；
> 新空 PostgreSQL volume、真实 `pg_restore`、证据重算、`RESTORE` 卷/secret 原子提交与覆盖升级
> 仍失败关闭；真实四方向 DataX E3、
> 主机/容器级默认拒绝 egress 的 Windows 实测、数据库外审计锚点/WORM、Authenticode
> 签名和干净 Windows 11 x64 E4 均为 `NOT_RUN/BLOCKED`。本文的 V1 能力描述是发布合同，
> 不是当前完成声明。

> **2026-08-02 问题基线**：第一性原理/对抗式复审已确认验收语义绑定、恢复中断、
> 核验取消、数据库职责权限、数据源外部 I/O 持锁、容器出口、Worker secret 闭包、运行中撤回止损、
> 凭据紧急终止、认证取证自举、发布签名供应链、安装路径重解析、Compose 容器归属、
> Docker 数据卷容量/cleanup、GitHub 远端治理和前后端契约等 P0/P1/P2 问题。实时状态、
> 关闭证据和开发顺序以
> [`14_第一性原理问题台账与开发修复计划.md`](./14_第一性原理问题台账与开发修复计划.md)
> 为准。P0/P1 未归零时，任何实现或绿色 CI 都不能改变发布 `BLOCKED`。

> **本轮对抗式安全审查**：源码级发现、修复、证据等级与未关闭门禁见
> [`security/security_best_practices_report.md`](./security/security_best_practices_report.md)。
> 报告中的 `FIXED_IN_SOURCE` 只表示 E1/E2 修复与回归通过，不表示 Windows E4、签名发布或
> 真实四方向 DataX 验收已经完成。

> **本机入口的资源边界**：Accepted ADR-0012 固定登录先准入后认证，并把审计链 readiness
> 从每请求全量重放改为持久水位线、受限重放和失败关闭；它只适用于固定的单 API 进程部署，
> 不允许以 Uvicorn workers、Compose scale 或旁路 API 入口扩展。该修复减少本机失控进程
> 造成的资源耗尽面，不能替代数据库外 WORM/签名锚点，也不构成 E3/E4。

> **GitHub-hosted Windows 预检**：
> [`security/windows-hosted-preflight.md`](./security/windows-hosted-preflight.md) 说明新增的
> Windows Server 原生编译/NSIS 语法 E1 上限预检。四次真实 online 失败曾安全暴露不可用的 Python
> patch、Windows 不支持的 Linux 开发依赖、错误假定 Windows 环境变量大小写敏感的测试，以及
> `Invoke-WebRequest` 返回的 NSIS 内容哈希错配；修复后的
> [run 30724285608](https://github.com/xiaoli2hust/datax/actions/runs/30724285608) 已完整通过。
> 即使如此，它也没有签名、发布、Docker/WSL2 或制品上传权限，因而不能替代 Windows 11 x64 E4、
> 签名或正式候选发布。

> **Windows 发布治理阻塞**：远端 `xiaoli2hust/datax` 目前为 public、User-owned 仓库，尚无
> `windows-candidate-signing` Environment 或 self-hosted runner；而 Accepted ADR-0010 强制的
> `datax-release-signing` custom runner group 需要 Organization/Enterprise 管理边界。迁入
> Organization（首选）或新的 Accepted ADR 是 `BLOCKED_DECISION`。即使后续创建 reviewer gate，
> 管理员绕过开关也不能由 REST/GraphQL verifier 读取，仍须保留 GitHub Settings UI 和组织审计
> 日志的外部证据。详见 ADR-0010 与 `14_`，在此之前不得发布 `Setup.exe`。

> ADR-0011 已接受两阶段插件/Runtime qualification 与最终发布晋级设计。当前仅有 P/QH
> Schema 与失败关闭 parser 的 E1 基础件；没有 QH private source/override、QR、受信 reader、
> Windows harness、签名或 OIDC 证据。它不改变当前 production deny-all、candidate-root.v1 的
> BLOCKED 语义或公开发布门禁。

## 1. 文档目的

本文件把原始方向提纲收敛为一套不相互矛盾的工程合同。V1 研发、测试和验收均以本基线为准；未来设想不得反向扩大 V1。

## 2. 权威来源与职责归属

文档不是一条可以跨关注点覆盖的全局优先级链。每类来源只在自己的职责范围内权威：

| 关注点 | 权威来源 | 不能做的事 |
|---|---|---|
| 产品结果、用户承诺、范围与非目标 | `01_产品战略与PRD.md`，以及变更这些承诺的 Accepted ADR | OpenAPI 或数据库字段不能自行扩大、缩小或改写产品语义 |
| 已接受的架构取舍与跨组件不变量 | `docs/adr/` 中状态为 `Accepted` 的记录 | 临时 Prompt、实现便利或单份编号文档不能绕过 |
| 跨文档术语与共同基线 | 本文件 | 不能替代具体 PRD、ADR、数据或 API 契约 |
| 持久化实体、约束和迁移 | `04_数据库与领域模型设计.md` | 不能重新定义产品成功、失败或权限承诺 |
| HTTP 与 JSON 的可交换结构 | `docs/contracts/openapi.yaml`、对应 JSON Schema 与 `05_API接口与前后端契约.md` | 机器可解析只证明结构有效，不证明产品语义正确 |
| 验收 oracle、证据和发布门禁 | `09_测试与验收标准.md` | 产品实现自身的统计或 DataX 自报结果不能充当独立真值 |
| 已确认问题、冲突状态、修复顺序和关闭证据 | `14_第一性原理问题台账与开发修复计划.md` | 不能代替 PRD/ADR/契约裁决语义，也不能用“修复中”代替关闭证据 |
| 具体页面与交互 | `02_产品信息架构与UI设计.md` | 隐藏按钮不能改变服务端权限或业务规则 |
| 临时工作说明 | Issue、PR 描述和 Prompt | 不能静默修改以上已接受合同 |

同一概念在任意两个来源中含义不一致时，实施状态立即标为 `BLOCKED`。负责人必须定位该概念的职责所有者，先同步修正 ADR、编号文档、机器契约、测试与追踪矩阵，再编码；不得选择最方便的解释，也不得让机器契约全局覆盖 PRD 或 ADR。

## 3. V1 产品定义

### 3.1 目标

为企业内部数据工程团队交付一个可在 Windows 11 x64 电脑上安装、可审计的安全一次性
离线全量复制控制平台，完成：

`登录 → 创建项目 → 管理并授权数据源 → 读取元数据 → 创建复制任务草稿 → 校验 → 发布版本 → 准备静默源表和外部独占的空目标表 → 手动执行 → 独立核验 → 审计追踪`

V1 不要求外部 Linux 服务器。用户下载发布方签名的 `Setup.exe`，安装同样签名的 Windows Launcher 和桌面
快捷方式；Launcher 检查 Docker Desktop + WSL2、幂等启动本机 Compose、等待健康检查，
再用系统默认浏览器打开 `http://127.0.0.1:17860`。Windows 11 x64 是唯一发布认证的
宿主平台；核心 API、Worker、PostgreSQL 与固定 DataX Runtime 仍运行在 Linux 容器中，
因此这不是纯原生单进程 `.exe`。

当前仓库所在的 Mac 只承担开发与自动化层验证；发布目标是另一台 Windows 11 x64
电脑。该 Mac 上的构建、容器或浏览器结果不能代替目标 Windows 上的安装、运行和 E4
证据。

宿主只映射 Web 的 `127.0.0.1:17860`，API、Worker、PostgreSQL 不映射宿主端口，也不
向局域网或公网提供入站访问。多项目与四类应用角色仍用于本机内的治理、职责分离和审计，
不代表支持其他电脑远程登录。数据库数据源访问是受 EndpointPolicy 约束的容器出站连接，
不得用它作为开放产品入站端口的理由。

一次 Execution 只承诺把一张从运行前检查开始到独立 oracle 完成始终静默的源表，完整
写入一张预创建、为空且可核验的目标表。Operator/DBA 必须提交有版本、有限有效期的目标
外部独占声明，并在知悉窗口被撤回或破坏时立即报告；TargetNamespace 锁只约束平台内
并发。人工声明不是数据库强制锁或技术证明，平台无法检测全部未报告或已经回滚的外部
DML/DDL。V1 不承诺对动态变化的源表建立数据库快照，也不承诺对同一目标表进行周期性、
增量或可重复同步。

### 3.2 目标用户

- `Admin`：系统设置、用户、项目成员、端点策略、数据源、方向授权、凭据策略和审计。
- `Developer`：在获得 `SOURCE_USE` / `TARGET_USE` 授权与有效 `TransferPolicy` 后读取元数据、设计复制任务、校验与发布；不能创建或修改网络端点和数据源连接。
- `Operator`：确认源表静默与目标外部独占、在知悉独占窗口被破坏时报告、执行、取消、观察三维结果状态，以及在外部完成目标处置后发起恢复后再次执行。
- `Viewer`：只读查看项目、任务、执行、日志和审计。

一个用户可拥有多个角色。所有权限由 API 服务端校验。

### 3.3 认证矩阵

| Reader | Writer | V1 状态 |
|---|---|---|
| MySQL 8 | MySQL 8 | 目标支持；当前真实 E3 `NOT_RUN` |
| MySQL 8 | PostgreSQL 15 | 目标主验收链路；当前真实 E3 `NOT_RUN` |
| PostgreSQL 15 | MySQL 8 | 目标支持；当前真实 E3 `NOT_RUN` |
| PostgreSQL 15 | PostgreSQL 15 | 目标支持；当前真实 E3 `NOT_RUN` |

“DataX 理论上支持”不等于本产品已认证。未列入矩阵的插件不得在 V1 UI 或 API 中启用。

### 3.4 支持的任务能力

- 单个 Reader、单个 Writer、单张静默源表、单张目标表。
- 一次性读取源表全部行，可复制全部字段或显式选列；一对一字段映射与顺序调整。
- Operator 必须确认源表从运行前检查开始到独立 oracle 完成都不再发生写入；确认值和时间进入审计。
- Operator/DBA 必须提交 `target_exclusivity_confirmation`，字段固定包含
  `statement_version="1.0"`、`confirmed_at`、`valid_until` 和 `responsible_party`，
  承诺从 Worker 最后一次空表实测到 oracle 目标一致性快照读完之间无平台外 DML/DDL。
  Execution 持久化 `ACTIVE / REVOKED / EXPIRED` 状态以及撤回时的 `revoked_at/reason`；
  知悉窗口被撤回或破坏时必须调用
  `POST /executions/{execution_id}/target-exclusivity/revoke`。只有状态仍为 `ACTIVE`、
  未撤回且目标快照 `finished_at <= valid_until` 时 oracle 才可 `PASSED`。确认和实际
  快照边界进入 Execution、RuntimeSnapshot 与审计；无法保证时拒绝运行。
- 上述声明是人工前提和报告通道，不是对外部数据库写入的技术证明。平台无法检测全部
  未报告或已经回滚的外部 DML/DDL；`TargetCopyLock` 只阻止平台自身对同一
  TargetNamespace 的并发工作。
- 发布只验证目标表存在、安全画像合格且具备可核验能力，不把发布时点的空表查询保存为执行证据。目标表必须在每次运行前为空；Worker 在启动 DataX 前服务端实测并保存本次 Execution 的 `target_empty_evidence`。平台不创建、修改、清空或删除业务表。
- 写入方式固定为 `insert-only`；非空目标表、不可核验目标表以及同一 TargetNamespace
  已有 `RESERVED/ACTIVE/RECOVERY_REQUIRED` 锁时拒绝创建运行。`QUEUED` 创建事务即预留
  目标，不能排入多个同目标意图后再让后者污染恢复门禁。
- 性能参数只开放经过边界校验的 `channel`，默认 `1`，范围 `1..16`。
- 脏数据容忍固定为 `0` 条、`0%`，不提供可调入口；任何脏记录都使执行无法成为已核验成功。
- 手动执行、取消、超时和恢复后再次执行。Execution 一旦被 Worker 领取，失败、取消、超时或 `LOST` 后必须先由 Operator/DBA 在平台外完成必要处置并提交处置确认，再由独立 RecoveryProbe 持目标锁复检目标为空，之后才能创建新的 Execution；尚未领取的 `QUEUED` 取消不创建 Attempt/fence/恢复门禁，并把创建时已有的 `RESERVED` 预留转为 `RELEASED`。
- DataX 退出码 `0` 只允许进入独立核验阶段；只有 oracle 通过后，执行才可成为已核验成功。

### 3.5 明确排除

V1 不支持：

- CDC、实时同步、自动增量、水位线和断点续传。
- 非空目标追加、周期性或可重复同步，以及从运行前检查开始到独立 oracle 完成仍持续变化的动态源表。
- Cron、DAG、依赖编排、补数编排和通知告警。
- 自动建表、DDL、覆盖写、upsert、truncate。
- 自由 SQL、`querySql`、`preSql`、`postSql` 和脚本转换。
- 文件、HDFS、NoSQL、云专有数据源和未认证插件。
- 插件上传、插件市场和第三方插件自动安装。
- AI 任务生成、自动诊断或自动调参。
- 多组织租户、计费、Kubernetes、多节点 HA 和异地灾备。
- 外部 Linux 服务器部署、局域网/公网远程使用、macOS/Linux 宿主发布认证。
- 纯原生单进程 Windows `.exe`、静默安装 Docker Desktop、自动接受或规避 Docker 许可。

### 3.6 Discovery Gate 与迁移边界

- 本文中的用户痛点和价值是待验证假设。企业试点或发布前必须完成 8–12 名目标用户访谈、
  至少 20 个脱敏真实任务样本分析，以及 3 个设计伙伴合计不少于 10 个真实一次性复制
  任务；证据必须包含失败案例和反证。未完成时 Discovery 状态为 `NOT_EVIDENCED`，试点、
  公开发布和 V1 完成声明均为 `BLOCKED`。
- 按 ADR-0007，仓库所有者可明确授权风险自担的工程候选实现；当前任务已有该授权。
  实现、测试和内部演示不计入 Discovery 证据，不得据此声称用户价值、试点或生产门禁
  已通过。研究结论变化时仍须回到 PRD/ADR/契约修订。
- `PRD-FR-JOB-010`（既有 DataX JSON 安全迁移）冻结为 `POST-V1`。V1 不提供导入入口；未来若立项，只能把认证白名单内的安全字段解析为新的 `DRAFT`，丢弃秘密、SQL、Transformer、自定义参数和未知插件，再重新完成授权、人工核对、校验与发布。

## 4. 技术决策摘要

| 主题 | 决策 |
|---|---|
| 前端 | Vue 3 + TypeScript + Vite + Element Plus |
| API | FastAPI 模块化单体 |
| Worker | 独立进程；唯一可启动/停止 DataX 和推进执行状态的组件 |
| 任务领取 | Worker 通过 PostgreSQL `FOR UPDATE SKIP LOCKED` 领取；`LISTEN/NOTIFY` 仅作可丢失提示，轮询兜底；V1 不依赖 Redis |
| 数据库 | PostgreSQL 15；SQLite 不属于支持路径 |
| 数据库运行角色 | 迁移 owner、`datax_api`、`datax_worker` 使用独立登录和密码；运行角色无 DDL/TEMP 权限，但当前两类运行角色仍有相同的全表 DML，细粒度状态写入边界主要由应用层执行 |
| Worker 数据库预算 | Worker 的控制、凭据和心跳共用一个 4 条、无 overflow、5 秒超时的 pool；`datax_worker` 角色 cap=12，PostgreSQL 在 30 秒空闲或 15 秒空闲事务后回收异常残留。该 backstop 不等于无限重试、HA 或 Windows E4 证据 |
| 宿主平台 | 仅另一台 Windows 11 x64 目标电脑通过 V1 发布认证；当前 Mac 只用于开发验证，不要求外部 Linux 服务器 |
| 安装与启动 | 发布版 Windows `Setup.exe`/Launcher 必须签名；安装器创建桌面快捷方式，Launcher 负责前置检查、幂等 Compose 启停、健康等待和打开默认浏览器 |
| 前置依赖 | Docker Desktop + WSL2；只检测并引导，安装器不得静默安装、替用户接受或规避许可 |
| Runtime | Alibaba DataX `datax_v202309`，JDK 8，制品/插件固定 SHA-256；固定在 Worker 的 Linux 容器内。PostgreSQL/egress-guard 固定 PostgreSQL `15.18-alpine3.24`，API/Worker 最终 Python 运行层固定 Python 3.12.13 `slim-trixie`（Debian 13） |
| DataX 完整能力 | ADR-0009 已决定保留完整上游并建立 `SOURCE_PRESENT → BUILD_VERIFIED → PACKAGED → CONTRACTED → E3_CERTIFIED → WINDOWS_E4_CERTIFIED` 单向认证；V1 产品范围只定义 MySQL/PostgreSQL 四个候选插件，当前普通用户实际开放为 0 个。未达精确最终 Windows E4 与公开晋级的能力不得对普通用户执行 |
| 插件认证门禁 | `plugin-manifest.v2` 是公开能力契约；Runtime 心跳只能证明当前制品最多 `PACKAGED`，当前生产默认 deny-all。ADR-0011 的 P/QH Schema 与失败关闭 parser 仅为 E1，尚无 QH private source/override、QR、受信 reader 或真实证据。未来仅可按不可变 payload → 受保护 QH → detached QR → 精确最终包 Phase B → ADR-0010 候选根/OIDC 的链路资格化；QH 只产生私有 harness 结论，绝不进入普通用户或公开 manifest，也不等于 Windows E4。受信 reader 必须同时绑定当前 candidate/commit/Worker image/Runtime/插件哈希、依赖与许可复核、精确最终 F 的 E3/E4、公开晋级和有效期；普通 Execution 创建、恢复 `rerun`、Worker 领取和启动四个检查点分别复检。当前没有任何 `WINDOWS_E4_CERTIFIED` 能力 |
| 网络入口 | 仅 Web 映射 `127.0.0.1:17860`；API、Worker、PostgreSQL 无宿主端口；不支持局域网/公网入站 |
| 部署 | Windows 本地单节点 Docker Compose；非 HA；睡眠、关机、重启或 Docker Desktop 停止时不可用 |
| 敏感运行区 | 含密 Job、未脱敏 Runtime 日志和 DataX 进程工作目录只进入 Worker 256 MiB tmpfs；持久 workspace 只保存不含秘密的有界 oracle 产物 |
| 系统备份 | DATA/SECRETS 分离加密导出、双包认证 journal、全新空 staging、`LEGACY` 指针提交与 Compose 消费已在 E1 自动化层验证；新空 PostgreSQL volume、真实 `pg_restore`、证据重算、`RESTORE` 卷/secret 原子提交、异机恢复和覆盖升级仍关闭 |
| 安全工作流 | PR 已配置 Gitleaks、hash-lock pip-audit、pnpm、Cargo OSV 和 CodeQL，并对真实 patched Worker 镜像执行 Syft+OSV 应用门禁；候选发布配置精确 commit 源码 SBOM、五镜像 SBOM、OSV 应用策略与 Grype distro 策略。工作流存在及本地证据不等于线上运行通过或主分支已强制保护 |
| 复制类型 | 安全的一次性离线全量复制 |
| 密钥 | 外部主密钥 + AEAD 密文；响应、日志、预览永不返回明文 |
| 时间 | 持久化 UTC；API RFC 3339；UI 按用户时区 |
| AI | V2 能力；Provider 可插拔；未配置时完整手工功能仍可用 |

初始 Admin 不通过公开注册接口创建。全新数据库完成迁移后，Windows Launcher 在本机
首次启动流程中调用容器内一次性离线 `bootstrap-admin`，临时密码只经标准输入传递，
不得进入进程参数、环境、日志或浏览器请求；命令只在系统尚无用户时可用，成功后写入
`SYSTEM_BOOTSTRAP_ADMIN` 审计事件。Launcher 不保存明文密码。后续账号全部由 Admin
API 管理。

## 5. 状态与一致性语义

### 5.1 任务

- `DRAFT`：可编辑，不可执行。
- `VALID`：当前草稿通过结构、权限和兼容性校验；继续编辑会返回 `DRAFT`。
- `PUBLISHED`：生成不可变 `JobVersion`，只有版本可被执行。
- `ARCHIVED`：不再创建新执行，历史记录保留。

发布不是覆盖旧版本。每个 `JobVersion` 都有规范化配置哈希。

### 5.2 执行的三个独立维度

执行结果必须同时保存和展示三个维度，不能用一个“成功”徽标代替：

| 字段 | 枚举 | 含义 |
|---|---|---|
| `process_state` | `QUEUED / STARTING / RUNNING / VERIFYING / SUCCEEDED / FAILED / TIMED_OUT / CANCEL_REQUESTED / CANCELED / LOST` | 控制面、DataX 进程和核验阶段的生命周期 |
| `data_effect` | `NONE / POSSIBLE / CONFIRMED / UNKNOWN` | 目标端影响的可知程度；`CONFIRMED` 表示影响已测得（可为 0 行），不表示内容正确 |
| `verification_state` | `NOT_STARTED / VERIFYING / PASSED / FAILED / INCONCLUSIVE` | 独立 oracle 对目标结果的判断 |

主路径为：

`QUEUED → STARTING → RUNNING → VERIFYING → SUCCEEDED | FAILED`

取消路径为：

`QUEUED | STARTING | RUNNING | VERIFYING → CANCEL_REQUESTED → CANCELED`

DataX 退出码为 `0` 后只能进入 `VERIFYING`；只有独立 oracle 数据比对通过，且目标外部
独占状态仍为 `ACTIVE`、没有 `revoked_at/reason`、目标一致性快照
`finished_at <= valid_until` 时，Worker 才能写 `process_state=SUCCEEDED`、
`data_effect=CONFIRMED` 和 `verification_state=PASSED`。声明已 `REVOKED`、已
`EXPIRED` 或快照越过截止时间时不得 `PASSED`。oracle 得到 `FAILED` 或
`INCONCLUSIVE` 时 `process_state=FAILED`。DataX 失败、取消、超时或状态丢失时，
`data_effect` 必须保守记录为 `POSSIBLE` 或 `UNKNOWN`，除非有可靠证据证明为 `NONE`。

只有 Worker 及其内置 reconciler 可写运行态、核验态和终态。API 可在同一事务插入初始
`QUEUED` Execution、`TargetCopyLock=RESERVED`、初始事件/审计/幂等事实及取消请求，但
不能直接宣告复制成功或失败。自动重试不进入 V1；机器接口可
为兼容保留 `rerun` 标识，但用户动作统一称为“恢复后再次执行”，且对任何已领取执行都
必须通过处置确认、独立 RecoveryProbe 空表证据、新的源静默确认、带新
`confirmed_at/valid_until` 的目标外部独占确认和目标互斥门禁。尚未领取的 `QUEUED` 取消由 reconciler 收敛为
`CANCELED/NONE/NOT_STARTED`，释放 `RESERVED` 预留且不创建恢复门禁。

领取事务提交前失败时，事务整体回滚，Execution 仍是从未领取的 `QUEUED`，创建时已有的
目标预留仍为 `TargetCopyLock=RESERVED`，但不存在 Attempt/fence，也没有 `ACTIVE` 锁转换；
后续领取只是首次有效领取。领取事务一旦提交，任何非唯一成功
结果都不得自动回到 `QUEUED` 或复用 Attempt，必须进入恢复门禁。

## 6. 术语

| 术语 | 定义 |
|---|---|
| Windows 本地工作站 | 一台 Windows 11 x64 电脑承载 Launcher、Docker Desktop/WSL2 和全部产品容器；只允许本机浏览器访问，不是外部服务器或 HA 节点 |
| Windows Launcher | `Setup.exe` 安装的本机控制壳；只做前置检查、Compose 启停、健康等待、首次引导和打开默认浏览器，不持有业务事实或运行 DataX |
| DataX Runtime | 固定版本、只读挂载的上游 DataX 程序及认证插件集合 |
| Datasource | 连接元数据与密文凭据的项目级资源 |
| SyncJob | 为保持机器标识兼容而保留的可编辑任务实体；面向用户统一称“复制任务”，不直接执行 |
| JobVersion | 一次发布产生的不可变、可执行任务快照 |
| Execution | 对一个 JobVersion 的一次运行请求 |
| TargetExclusivityConfirmation | `statement_version="1.0"` 的有限期人工声明；状态为 `ACTIVE / REVOKED / EXPIRED`，知悉窗口破坏时必须报告；不是外部写入未发生的技术证明 |
| TargetCopyLock | 对同一 TargetNamespace 的平台内排队、运行和恢复工作做串行化；不能阻止或证明不存在平台外 DML/DDL |
| ExecutionEvent | 运行状态的追加式事件 |
| LogChunk | 带单调游标的脱敏日志块 |
| Plugin Manifest | 平台维护的插件能力、参数、敏感字段和兼容版本清单 |
| JobSpec | 与上游 DataX JSON 解耦的平台任务契约 |
| 独立 oracle | 不复用 DataX 退出码、自报写入数或产品汇总逻辑，对静默源表与目标表执行规范化计数/摘要核验的验证器 |
| 真实 E2E | 固定 Runtime 对真实测试数据库执行一次性全量复制，并由独立 oracle 验证源/目标数据的证据 |
| 企业试点 | 可受控安装和审计，但不等同于 HA 或生产认证 |

## 7. 需求编号

- `PRD-FR-*`：功能需求。
- `PRD-BR-*`：业务规则。
- `NFR-*`：非功能要求。
- `SEC-*`：安全/隐私控制。
- `AI-*`：后续 Agent 要求。
- `ACC-*`：验收用例。

新增或删除需求时必须同步 `12_需求追踪矩阵.md`。

## 8. 文档变更控制

以下变更必须新增 ADR：

- V1 范围或认证数据源变化。
- 模块化单体/Worker/队列/事实源边界变化。
- 执行三维状态、恢复后再次执行、幂等、一次性复制或版本语义变化。
- 凭据、RBAC、审计、网络或进程隔离变化。
- Windows 认证平台、安装/Launcher、Docker Desktop/WSL2 前置依赖、宿主端口或本机
  数据生命周期变化。
- 新增任意 SQL、插件安装、AI 工具执行或生产自动化。

每个变更 PR 必须说明：动机、影响的需求 ID、迁移、向后兼容、安全影响、测试和回滚。

## 9. 未决但不阻塞 V1 编码的所有者决策

| 决策 | 当前安全默认 | 截止门禁 |
|---|---|---|
| 本仓库原创代码许可证 | 未授予开源许可，不放置 `LICENSE` | 首次公开源码发布前 |
| Docker Desktop 许可适用性 | 安装器只检测和链接官方指引，不分发 Docker、不替用户接受许可；由使用者/组织自行评估并接受 | 首次 Windows 安装验收前 |
| Windows 安装器代码签名证书 | 开发构建可未签名但必须明确警告；对外分发前由所有者提供受信任证书并完成签名验收 | 首次对外分发 `Setup.exe` 前 |
| 产品 Logo/品牌色 | 使用中性占位标识，不复制第三方商标 | UI 品牌验收前 |
| 生产 SSO 方案 | V1 仅本地账号 | 后续远程/生产形态单独立项前 |

以上项目不是编码 Agent 可自行替用户决定的事项。

## 10. 基线完成判定

“文档齐全”要求：

- 全部编号主题文档均有明确范围、异常和验收，不只是标题。
- OpenAPI 和 `docs/contracts/` 下全部 JSON Schema 可被解析、引用和校验。
- PRD、UI、数据模型、API、测试之间有需求追踪。
- 架构、部署、安全和 Agent 边界无冲突。
- Windows 11 x64、`Setup.exe`/Launcher、Docker Desktop + WSL2、Linux 容器和
  `127.0.0.1:17860` 的边界在 PRD、ADR、部署、安全、测试与契约中一致。
- Prompt 不允许以 Mock 或静态页面冒充真实闭环。

“产品完成”则必须另行通过 `09_测试与验收标准.md`，文档完成不能替代产品验收。
