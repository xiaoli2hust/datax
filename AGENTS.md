# AGENTS.md

本文件适用于仓库全部目录。任何人或编码 Agent 在修改代码、文档、部署文件或契约前都必须遵守。

## 1. 开始前的阅读顺序

1. `README.md`
2. `docs/00_文档总览与决策基线.md`
3. 与任务相关的编号文档
4. `docs/adr/` 中已接受的架构决策
5. `docs/contracts/` 中对应的机器契约
6. 当前 Issue/PR 的验收标准

权威性按关注点归属，不使用一条全局覆盖顺序：

- PRD 对用户价值、范围、业务结果和业务规则负责。
- Accepted ADR 对跨模块不变量、取舍与不可逆架构决策负责。
- 数据模型文档对持久化语义和约束负责。
- OpenAPI/JSON Schema 对线上传输形状、枚举和机器校验负责。
- 测试与追踪矩阵对验收 oracle、证据和发布门禁负责。

任何两个来源对同一关注点给出不同结论，都视为阻塞项。必须先建立冲突记录、确定责任文档并同步修正所有派生文档和机器契约；不得让机器契约静默覆盖产品语义，也不得自行选择最方便的解释。

## 2. V1 不可漂移边界

- V1 是手动触发的安全一次性离线全量复制，不是周期同步、重复同步、增量同步或实时同步。
- 仅认证的 MySQL 8/PostgreSQL 15 Reader/Writer。
- 仅完整表或选列直连映射；源表必须由 Operator 确认从运行前检查开始到独立 oracle 完成始终静默；目标表必须预存在、为空且可核验。Operator/DBA 必须提交包含
  `statement_version="1.0"`、`confirmed_at`、`valid_until`、`responsible_party` 的
  `TargetExclusivityConfirmation`；其状态为 `ACTIVE / REVOKED / EXPIRED`。Worker/oracle
  只有在状态仍为 `ACTIVE`、`revoked_at/reason` 为空且目标一致性快照
  `finished_at <= valid_until` 时才可写 `PASSED`。
- 目标外部独占声明是带报告义务的人工前提，不是技术证明；平台无法检测全部未报告或已经
  回滚的外部 DML/DDL。Operator/DBA 知悉窗口被撤回或破坏时必须调用
  `POST /executions/{execution_id}/target-exclusivity/revoke`，持久化 `revoked_at/reason`。
  `TargetCopyLock` 只串行平台内工作；仅 `insert-only`。
- 源静默确认同样是人工承诺；平台只以核验前后指纹不变作为必要证据，不能把它包装成
  数据库一致性快照，也不能证明未报告或瞬时后回滚的源端写入从未发生。V1 未定义源确认
  的过期/撤回生命周期，不得在测试或实现中虚构相关字段和状态。
- V1 脏数据容忍固定为 `0` 条、`0%`；任何脏记录都不能成为已核验成功。
- 非空目标追加、自动清空目标、自动回滚、自动去重和失败后无人工处置的自动再次执行均不属于 V1。
- 仅手动触发；无 Cron、DAG、告警、插件市场、CDC。
- 无任意 SQL、`preSql`、`postSql`、脚本转换或用户上传插件。
- 单组织、多项目、四类角色；单节点 Docker Compose，非 HA。
- AI 不属于 V1 运行依赖，不得为了页面效果接入假模型结果。

变更以上任一条，必须先新增或更新 ADR，并同步修改 PRD、API、数据模型、测试和部署文档。

## 3. 架构约束

- FastAPI 为模块化单体，不得擅自拆微服务。
- API 进程不得直接启动 DataX；只能持久化执行请求并入队。
- Worker 及其内置 reconciler 是唯一可推进运行态/终态并管理 DataX 进程树的组件；API
  只能在一个事务创建初始 `QUEUED` 记录及其 `TargetCopyLock=RESERVED`、记录取消请求，
  或持久化目标外部独占撤回/窗口破坏报告；API 不得借这些接口推进三组执行状态。Worker
  领取时才把预留转换为受 fence 保护的 `ACTIVE`。
- PostgreSQL 是任务、版本、执行、事实队列、日志索引和审计的事实源；V1 不依赖 Redis。Worker 使用 PostgreSQL 原子领取，`LISTEN/NOTIFY` 只作可丢失唤醒并以轮询兜底。
- 每次执行绑定不可变 `JobVersion`、源/目标 `DatasourceRevision` 与
  `EndpointPolicyRevision`、物理端点/表身份、TransferPolicy `scope_hash`、本次实际
  CredentialSecret/Envelope、连接证据、配置哈希、Runtime 和插件版本；凭据轮换不改变
  非秘密连接配置修订。
- 每个 TargetNamespace 同一时刻只允许一个未释放的排队预留、活动执行或恢复锁；Worker
  的领取、心跳、状态写入和进程身份必须受单调 fencing epoch 保护。
- DataX 退出码为 0 只表示进程完成；必须经过独立数据 oracle 核验后，Execution 才能进入业务成功状态。
- 外部进程必须使用参数数组启动；禁止 `shell=True`、字符串拼接命令或用户可控路径。
- 每次执行使用独立工作目录、最小权限用户、资源限额和可清理的临时凭据文件。

## 4. 数据与 API 规则

- 先改契约，再改实现；OpenAPI 和 JSON Schema 必须与代码一起通过校验。
- 数据库结构只能通过可回滚迁移变更，禁止运行时自动改表。
- 时间统一存 UTC，API 使用 RFC 3339；展示层按用户时区转换。
- 列表接口必须分页并有稳定排序；写接口按契约支持幂等或乐观锁。
- API 错误使用稳定错误码、`request_id` 和安全的人类可读信息。
- 执行结果必须分别表达 `process_state`、`data_effect` 和 `verification_state`；不得用一个 `SUCCEEDED` 掩盖未核验、部分写入或影响未知。
- 密码、连接串、Token、主密钥和完整 DataX 配置不得出现在响应、日志、测试快照或 Git 历史。
- 删除项目、数据源、任务等操作优先软删除；有历史执行引用时禁止物理删除。

## 5. 不能伪装

禁止以下行为：

- 用 Mock、固定 JSON、静态日志或定时器伪装真实 DataX 执行。
- 在未安装 Runtime、未连接真实测试数据库时声称 E2E 已通过。
- 把“页面可点击”称为功能闭环。
- 把自动测试、Docker 构建或包存在称为生产验收。
- AI Provider 未配置时返回伪造的诊断或任务草稿。
- 吞掉错误并显示成功，或用演示数据覆盖真实失败状态。

若环境缺少外部依赖，应明确记录 `BLOCKED`、缺失项、复现步骤和已验证范围，保留手工路径，不得降级成假成功。

## 6. 安全与隐私

- 数据源元数据、日志和表名都属于不可信输入；必须防止注入、路径穿越和提示注入。
- 凭据只以密文或 Secret 引用持久化，运行时按需解密，使用后清理。
- `Admin / Developer / Operator / Viewer` 权限必须在服务端执行；隐藏按钮不是权限控制。Admin 管理允许的数据源端点、用途和源到目标传输策略，Developer 只能引用已获授权的数据源修订。
- 主机名解析、连接探测、元数据读取和 DataX 进程必须受到同一套 DNS 重绑定防护与网络出口策略约束。
- 所有凭据、权限、发布、执行、取消和删除操作必须产生审计事件。
- 日志只保存“脱敏后的原序日志”并限制大小；任何截断都必须显式记录原始字节数、丢弃字节数和原因。
- 新增网络访问、文件系统访问、任意 SQL、插件安装或 AI 工具调用时，必须更新威胁模型。

## 7. 测试与完成定义

每个变更至少执行与风险相称的测试：

- 纯函数和业务规则：单元测试。
- API/Schema：契约测试和错误路径。
- 数据库：迁移正向、回滚或恢复验证。
- 执行器：进程、超时、取消、崩溃对账和日志脱敏。
- 主链路：真实 MySQL/PostgreSQL + 固定 DataX Runtime E2E。
- 数据正确性：使用独立、版本化的 oracle，明确 NULL、Unicode、Decimal、时区、重复行和
  多重集哈希规范；源端以外部静默确认及核验前后指纹不变为前提，目标端 count 与摘要
  必须来自同一一致性读事务并记录 snapshot ID 和起止边界。
- 恢复路径：验证部分写入、非空目标、并发同目标、源端变化、处置确认和恢复后再次执行前的 Worker 复检。
- 权限/凭据：越权、泄露和审计测试。
- UI：关键流程、空态、失败态、权限态和可访问性。

完成必须同时满足：实现、测试、文档、迁移/配置、真实证据、已知限制均已交付。需求优先级、唯一测试 ID、oracle、证据路径和结果必须进入机器可读验收清单；测试命令与结果写入 PR，未执行项必须明确标注。

## 8. 变更纪律

- 保留用户已有改动，不覆盖无关文件。
- 只暂存当前任务文件；提交信息简短并能描述完整意图。
- 依赖必须锁定版本；新增依赖需说明用途、许可证和替代方案。
- 不提交密钥、生产数据、数据库转储、运行日志或大型 Runtime 二进制。
- 实现发生行为变化时，同一 PR 更新相关编号文档、契约和追踪矩阵。
- 每个 PR 保持单一目的；大改动拆为可独立验证的纵向切片。

## 9. 交付报告

交付时用事实区分：

- 已修改的文件和行为；
- 已执行且通过的检查；
- 真实外部链路是否验证；
- 未验证、受阻或计划后续处理的内容；
- 数据迁移、兼容性、安全和回滚影响。

除非所有 V1 发布门禁通过，否则不得使用“生产就绪”“企业级已完成”等表述。
